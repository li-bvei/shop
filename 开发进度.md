# 店铺管理系统 — 开发进度

最后更新：2026-09-06

## 2026-09-06 运营反馈批次 + 机构级功能开关 + 平台运营控制台（已上线）

本轮从用户实际使用中的 9 项反馈起，滚了 3 轮，最后整体部署到生产
（`43.132.201.45` `/www/wwwroot/shop`，docker compose，站点
`http://43.132.201.45:8082`，未走宝塔 nginx）。全程真机浏览器验证；收尾时
后端全量套件 **398 项通过**，前端 `vue-tsc` / `eslint` / `build` 全绿，
zh/ja 文案 1147 键对齐。生产上迁移已跑、种子已跑、超级管理员账号已建。

提交：`947b92b`（主批次）、`030cd3e`（打卡档位，迁移 0009）、
`3f26d4d`（nginx 缓存修复）。

### 已完成

1. **日报金额千分位**：新增 `components/MoneyInput.vue`——聚焦时显示纯数字方便编辑，
   失焦后显示 `¥1,234,567`，绑定值仍是 `number`。营业额/上午营业额/各支付方式/
   报销金额输入框都换成它；只读的下午营业额/现金派生框改用 `formatCurrency`。
2. **カウンター受付 点击开新标签页**：侧栏该项从 SPA 内跳转改成
   `<a href="/kiosk/verify" target="_blank">`，后台页面不再被顶掉。见 `AppSidebar.vue`。
3. **客户卡认证是否要过期（只答疑）**：`card_token` 是 `secrets.token_urlsafe(16)`
   的不可猜、不轮换令牌，本来就不过期——信任模型是"顾客对抗、店员诚实"，所有价值
   锁在店员当面核销。性能无影响（每请求一次带索引的单表查询）。后台 JWT
   （access 8h / refresh 7d 轮换）按用户意见本轮不动。
4. **活动按星期/日期区分**（扩展 `Campaign`，迁移 0008）：新增
   `active_weekdays`（ISO 星期数字串，默认 `1234567`）、`active_date_from/to`
   （营业日区间，黄金周用）、`priority`。一个门店可同时有多个 active 活动，
   核销/打卡时 `services.resolve_active_campaign` 按"当天命中 + 优先级最高"选一个。
   `PromotionsView` 活动表单加了星期多选、生效日期区间、优先级。
5. **每日打卡奖励 + 累计次数档位**（迁移 0008/0009）：
   - `Campaign.checkin_reward_*`——当日首次出示二维码（付款核销或独立"来店打卡"）
     自动发一张券。默认关闭。
   - 累计次数档位 `CheckinMilestone` / `CheckinMilestoneClaim`（仿 `Milestone`，
     但按 `CheckInRecord` 总数而非积分）：**打 3 次→甜品券、打 5 次→¥100 券、
     有效期 7 天**，每档只发一次。`services._apply_checkin_milestones` 在
     `verify_spend`（当日首次）和 `record_checkin` 里触发。
   - 新端点 `POST /api/promotions/spend-verifications/checkin/`（无消费打卡）+
     `CheckinMilestoneViewSet` + Django admin inline。
   - **种子命令 `seed_checkin_reward`**（不受 DEBUG 限制、幂等）：默认铺
     3→甜品 / 5→¥100 / 7 天；参数 `--org --branch --expires-days --dessert-at
     --voucher-at --voucher-yen --daily-reward`。生产上跑了 `--org 1`。
6. **カウンター受付 / クーポン利用 平板相机扫码**：新增依赖 `jsqr`（懒加载，
   ~130KB 独立 chunk，不进首屏）；新组件 `components/QrScanner.vue`——`getUserMedia`
   + 逐帧 jsQR 解码，纯页内 overlay，不跳转、不调起相机 app，无权限时优雅回退手动输入。
   两个柜台页的扫码阶段都加了"📷 相机读取"按钮。iOS Safari/Chrome 没有原生
   `BarcodeDetector`，所以自带解码器。
7. **月次経営去掉「臨時／時給スタッフ給与」**：前端删掉该行 + 工资锁定状态卡 +
   Excel 导出行；后端 `dashboard/analysis.py` 的 `暫定経営差額` 不再减工资，
   去掉"临时工工资占比"那条自动分析。**独立的「工资计算」功能整块没动。**
8. **机构级功能开关 + 平台超级管理员**（迁移 organizations 0004）：
   - `organizations.OrganizationFeature(org, feature, enabled)`——**无记录 = 开启**，
     存量机构不受影响。`common/features.py` 是模块注册表 + 路径→模块映射，
     `common/middleware.OrganizationFeatureGateMiddleware` 对被关模块的
     `/api/` 前缀返回 403 `feature-disabled`（中间件自己解 JWT）。可控模块：
     products / inventory / purchasing / suppliers / scheduling / wages /
     promotions / lottery。核心（看板、日报、员工、设置）恒开。
   - 一个机构关掉某模块，**该机构下所有账号（本部/分店/员工）都同步失效**——
     分店无法自行恢复。`/auth/me` 现在返回 `enabledFeatures` + `isSuperuser`。
   - 超级管理员 = Django `is_superuser`，跨所有机构。**登录后被锁在 `/platform`
     控制台**（`router.homeRoute()` 对 superuser 返回 `platform-overview`，
     `beforeEach` 把非 `/platform/` 路由弹回）——看不到单店的日常运营界面。
   - `/platform/overview`「全体概要」：跨租户数据——总机构/门店/账号数、本月
     营业额（含环比）、各机构一行（门店数、启用/全部账号、启用模块数、本月
     营业额/客数/进货、会员数、进行中活动数）。`GET /api/platform/overview/`。
   - `/platform/manage`「运营管理」：按机构开关模块 + 展开看该机构账号、
     一键启用/停用账号。端点在 `/api/platform/` 下。
9. **总店账号扫码报错文案**：本部账号（admin、无分店）在柜台页返回编码
   `head-office-account-cannot-scan`；前端 `/kiosk/verify` 和 `/kiosk/redeem`
   打开时直接全屏提示（日文为主）「総本部アカウントではスキャン受付・クーポン利用は
   できません。各店舗のアカウントでログインしてください。」+ 中文一行。

**账号停用（第 8 项衍生）**：用 Django 自带 `User.is_active`（无迁移）。
`accounts/services.guard_account_deactivation`——不能停自己、不能停机构最后一个
启用的 admin。本部在「设置→账号管理」有状态开关列；超级管理员在「运营管理」里
每个机构的账号列表也能开关。simplejwt 在 `is_active` 翻 false 的下一个请求就
拒绝活着的 access token（`get_user` 里有检查），即时生效。

**部署根因修复 `3f26d4d`（`store-admin-frontend/nginx.conf`）**：
用户报"部署后页面打不开、提示报错"——根因是浏览器缓存了旧 `index.html`
（nginx 没发 `Cache-Control`），里面记的是旧的 chunk 哈希；重建后这些文件没了，
请求它们时 `try_files` 回退到 `index.html` 当结果返回，浏览器把 HTML 当 JS 加载
→ `import()` 抛异常 → "予期しないエラー" toast，页面加载不出来。改法：
`index.html` 加 `Cache-Control: no-cache`（每次校验、命中返 304）；`/assets/`
里缺失的文件返回干净的 404（`try_files $uri =404`）而不是回退 HTML，
命中的哈希文件 `immutable` 缓存 1 年。**以后每次部署都不会再出这个问题**，
但已经缓存了旧页面的浏览器需要各自硬刷新一次。

### 已测试

- 后端：`manage.py test` **398 项全绿**（含新增：`campaign_applies_on` / 优先级
  选活动、打卡发券幂等、3/5 次档位触发、`seed_checkin_reward` 默认档位、
  `FeatureEnabled` 403、平台端点仅超管、账号停用即时失效与守卫、
  `build_monthly_analysis` 不再含 wage、`build_platform_overview` 跨机构计数）。
  迁移 `promotions 0008/0009`、`organizations 0004` 已在生产库执行。
- 前端：`vue-tsc --build`、`eslint src/`、`npm run build` 全绿；zh/ja 1147 键对齐。
- 真机浏览器：日报金额输入千分位；月次経営无工资行；本部账号打开柜台页显示日文
  拦截；相机扫码 overlay 打开（无摄像头环境退回手动）；活动编辑弹窗新字段渲染；
  超级管理员登录落到「全体概要」、被锁出 `/dashboard` 等、看到 2 个机构并能开关
  模块 + 停用账号；关掉某机构 inventory 后其分店账号侧栏无「在库管理」、
  强行访问链接落到 feature-unavailable 页 + API 403。
- 生产验证：`/auth/me` 新字段、admin 访问 `/platform/overview` 403、
  打卡档位数据、`/platform/overview` 实时数（本月营业额 ¥971,200、3 门店、
  5 账号、2 会员、3 活动）、nginx 缓存头与旧 chunk 返回 404。

### 生产部署状态

| 项目 | 结果 |
| --- | --- |
| 迁移 | organizations 0004、promotions 0008 + 0009 已跑 |
| 打卡档位种子 | `seed_checkin_reward --org 1`——3 门店的活动都铺了 3→甜品 / 5→¥100 / 7 天 |
| 超级管理员 | `platform` 账号已建（`is_superuser=True`、`is_staff=True`、org=1、密码用户自设） |
| nginx 缓存修复 | 已拉取 + 重建 + 验证生效 |

### 已知限制 / 本轮刻意未做

- **打卡次数档位（3/5 次）目前只能通过种子命令 / Django admin / REST 管理**，
  `PromotionsView` 里还没有对应的增删改弹窗——快速跟进项。
- 种子命令用 `--org 1`（全机构），因为 namba / umeda 之前没有活动，命令给它们
  各建了一个空的「来店チェックインキャンペーン」（campaign 2/3，同样铺了档位）。
  心斎橋（campaign 1）是在原活动上追加，没动它的积分设置。这两个空活动没有任何
  客户，用户确认保留（以后 namba/umeda 也做会员就直接用；不做就删掉也安全）。
- 付款核销路径也会触发次数档位，但柜台"确定"画面只显示积分、不播报档位券
  （券已在顾客手机的卡片页，是"下次到店"券、不影响当前账单）；独立"来店打卡"
  画面会用 🎉 播报。
- **浏览器最低要求**：Vue 3 依赖 ES2015 `Proxy`（无法 polyfill），加上 Vite
  默认打包目标 ≈ Safari 16，**iOS 9/旧 Safari 打开是空白**，这不是 bug、改配置
  也救不了。实际最低 ≈ iOS 14+（iPad Air 2 及以后）或任意现代 Android。
  若要兼容 iOS 12/13 可加 `@vitejs/plugin-legacy` + 降 build target（另立项）。

## 2026-08-21 按 emilkowalski/skills 设计评审（当前状态）

按用户要求，用 [github.com/emilkowalski/skills](https://github.com/emilkowalski/skills.git)（`emil-design-eng`/`review-animations`/`apple-design` 等技能，Emil Kowalski——Sonner 作者、原 Vercel/Linear 设计工程师——的动效与界面打磨方法论）对前端项目做了一次系统性检查。方法：`git clone` 该仓库到本地，读取 `emil-design-eng`（核心检查清单：过渡属性精确化、`ease-in`禁用、`scale(0)`禁用、popover 需 `transform-origin` 对齐触发点、时长<300ms、按钮需按压反馈等）和 `review-animations`（十条硬性标准+审查方法）两份技能文档，再对全项目 `src/` 下所有 `.vue` 文件里的自定义 CSS 过渡/动画逐一核对（Element Plus 组件自带的过渡不在此次核查范围内，因为那是第三方库已经打磨好的行为，不应该被项目自定义样式覆盖）。

**结论**：项目现有的自定义动效整体是干净的——没有 `transition: all`、没有 `scale(0)` 入场、没有裸 `ease-in`、没有超过 300ms 的 UI 过渡、没有对 Element Plus 的 popover/dropdown/tooltip 做 `transform-origin` 之类的破坏性覆盖，`AppShell.vue` 的页面切换过渡（140ms opacity ease-out）已经正确处理了 `prefers-reduced-motion`。找到并修复了 4 处具体问题：

1. **`PurchasingView.vue` 的"已保存"徽标动画**（本轮功能，见下方 2026-08-21 用户反馈批次一节第6项）：入场过渡用的是 `ease` 而非规范要求的 `ease-out`（入场/出场元素必须用 `ease-out`，`ease` 起步太慢，不够干脆）；离场淡出用了 `0.5s`，超过"UI 动效应控制在 300ms 内"这条硬性标准。已改为入场/离场统一 `ease-out`，离场时长压到 `0.28s`。
2. **`DailyReportForm.vue` 的"添加一行"/快捷建议标签**（`.add-row-btn`/`.suggest-tag`）：这两个手写的可点击 `<span>` 完全没有按压反馈，违反"任何可按压元素都要有响应"这条组件构建原则（Element Plus 的 `el-button` 自带这个行为，但这两处是项目自己手搓的，没有继承到）。已补上 `:active { transform: scale(0.95~0.97) }` + `120ms ease-out` 过渡。
3. **`PurchasingView.vue` 的"取消编辑"链接**（`.cancel-edit`）：同一文件里紧邻的 `.item-name-link` 有 hover 下划线反馈，`.cancel-edit` 却完全没有任何 hover 状态，两个视觉上都是"可点击文字"的元素处理不一致。已补上 `120ms ease` 的文字颜色过渡。
4. **`SchedulingView.vue` 里一段死代码**：`.desktop-only`/`.mobile-only`/`.mobile-employee-card`/`.mobile-employee-header`/`.mobile-day-strip`/`.mobile-day-cell`/`.mobile-day-number`/`.mobile-day-state` 共 8 条 CSS 规则在模板里已经完全没有引用（应该是早期某版移动端卡片视图的遗留，后来被现在的横向滚动表格取代但没清理样式）。已整段删除。

**顺带修好的相邻问题**（不是 Kowalski 技能范围内的检查项，是验证过程中开发服务器报出的真实警告，一并处理了）：`DailyReportView.vue`、`MonthlyAnalysisView.vue`、`staff/MyWagesView.vue` 的打印区域里一共 8 张 `<table>` 缺少 `<tbody>`，`<tr>` 直接挂在 `<table>` 下——这不符合 HTML 规范，Vite/Vue 编译期会报 `<tr> cannot be child of <table>` 警告（"can cause hydration errors or potentially disrupt future functionality"）。其中 `MonthlyAnalysisView.vue` 的 3 处是本轮（见下一节第4项）新写的代码，`DailyReportView.vue`/`MyWagesView.vue` 的 5 处是更早之前留下的。全部补上 `<tbody>` 包裹，已用脚本扫描确认 `src/` 下再无同类问题（`grep -rn '<table' --include=*.vue . `逐个核对过闭合结构）。

**明确评估过、判断为"已经合规、不需要动"的地方**（记录下来避免下次评审重复劳动）：

- `SchedulingView.vue` 排班格子（`.cell`）的 `background-color 0.1s` 状态反馈：这是一个高频点击元素（排一整月班可能点几十上百次），Kowalski 清单里"100+次/天的操作不该有动画，几十次/天的应该精简"这条恰好适用——现在已经是最小化的纯色变过渡，没有额外加按压缩放，是刻意的正确选择，不是遗漏。
- `AppSidebar.vue` 的 `background-color 0.25s`：这是整个侧栏在切换深浅色主题时的过渡，不是 hover 触发，不受"hover 动效必须加 `@media (hover: hover)` 网关"这条约束。
- 各类可点击 `<el-table__row>`（月度经营明细跳转、报表修改历史）：复用 Element Plus 表格自带的行 hover 高亮，未额外定制，符合"不要重复造轮子、信任已打磨过的库行为"的原则。
- `.avatar-trigger`（顶栏头像菜单触发器）：包在 `el-dropdown` 里，下拉菜单的入场/出场动效由 Element Plus 接管，触发器本身不需要额外的按压反馈——低频操作（偶尔点一次退出登录），符合频率分级里"偶发操作用标准动效即可"的判断。

**验证**：全部改动为纯 CSS/模板结构调整，无逻辑变化。`type-check`/`oxlint`/`eslint --no-cache`/`build-only` 全部通过；真实浏览器里核对了 `.saved-pulse-badge`/`.cancel-edit`/`.add-row-btn`/`.suggest-tag` 四处新样式规则已正确编译进最终样式表（通过 `document.styleSheets` 读取确认，含 scoped 属性选择器），`DailyReportForm.vue`/`DailyReportView.vue` 页面渲染正常无破版。

## 2026-08-21 用户反馈批次修复

本轮是针对用户实际使用后提出的 7 项具体反馈的修复，不是新功能开发；不涉及重新建模或架构调整。每一项都在本机真实浏览器里操作验证过（含创建测试数据后清理），并跑过前端 type-check/oxlint/eslint/build 与相关 Django 测试模块。收尾时跑过一次全量套件：**前端构建全绿，后端 235 项测试全部通过**。

### 已完成

1. **进货页选供应商后卡顿**：根因是 `GET /api/purchases/suggestions/` 原来对每个不同商品名单独跑一次关联子查询取"最新单价"（无索引支撑，供应商进货记录一多就是 N+1 式的性能坑）。改为单次 `order_by('-date','-id')` 限量取最近 500 条，在 Python 里按商品名聚合。实测同一供应商（1926 条记录）响应时间从 2.23s 降到 0.02s。见 `purchasing/views.py:PurchaseRecordViewSet.suggestions()`。
2. **页面下方悬浮的"vue"图标**：是 `vite-plugin-vue-devtools`（仅开发环境注入，生产构建里从不出现），已从 `vite.config.ts` 移除并 `npm uninstall`。
3. **排班表格与实际工作确认**：
   - 格子状态循环新增反向操作：左键正向循环 休→上午→下午→全日，右键反向循环，全日现在 1 次点击可达（原来最多要点 3 次）。见 `SchedulingView.vue:cycleCell`/`reverseCycleCell`。
   - 实际工作确认表格新增删除操作。**关键约束**：`generate_actual_records` 是按 `(employee, work_date)` 幂等 `get_or_create` 的，只要该员工当天的排班班次还在，物理删除这条记录在下次保存排班草稿时会被自动重新生成——所以删除按钮的行为按记录是否关联排班分两种：
     - 有关联排班的记录：标记为"缺勤"（`absent=true`，清空实际起止时间），这个标记是稳定的（`get_or_create` 永远不会覆盖已存在的行），且工资计算引擎本来就过滤 `absent=False`，所以缺勤记录不会计入工资。
     - 无关联排班的手工/临时记录（`shift=null`）：真正物理删除（`DELETE /api/actual-work-records/{id}/`，后端早已支持，只是前端一直没有暴露这个按钮）。
   - 顺带修好一个已存在但未被察觉的表格布局 bug：操作列用了 `fixed="right"`，但表格容器宽度不够，导致固定列的覆盖层把状态列的文字盖住了。去掉 `fixed="right"` 后恢复正常滚动/换行。
4. **月度经营打印空白页**：原打印区域只有一个精简的两表格摘要，容易因为洞察列表长度不定而溢出到第二页（哪怕第二页几乎是空的，观感上就是"打印出来是空白的"）。补充了 KPI 卡片网格、次要统计表、供应商采购排行（前6）区块，并把洞察列表也限制在前6条，保证整体高度稳定可控。实测渲染高度约 693px，远小于 A4 单页可用高度（约1032px @96dpi），不会再溢出到第二页。
5. **每日报表**：
   - "负责人"下拉现在只列 `workArea === 'hall'`（前厅）的员工，厨房员工不再出现在候选里（其余展示/历史记录的员工姓名查找逻辑不受影响，仍能正确显示所有员工）。见 `DailyReportForm.vue:hallStaff`。
   - 整体视觉重做：营业额/客数/组数等录入字段从平铺的输入框改成带背景/圆角/分区的"卡片式"输入区，支付方式明细与报销明细两栏各自套上独立的卡片容器（原来只是一条竖线分隔），标题统一加图标，风格与看板/月度经营等页面的卡片语言保持一致。
6. **进货录入保存无提示**：提交成功后在输入提示文字旁淡入一个小的绿色"已保存"徽标（1.4秒后自动淡出），不弹全局 toast、不抢焦点，连续按回车批量录入的手感不受影响。
7. **供应商打印**：
   - 新增 `bank_account_furigana`（账户名义人假名）字段：后端 `Supplier` 模型 + 迁移 `0006_supplier_bank_account_furigana` + 序列化器；前端表单新增输入项，打印表里显示在汇款账号下方（小字灰色）。
   - "电话"从必填改为选填——后端 `Supplier.phone` 本来就是 `blank=True`，问题完全在前端表单校验规则（`rules.phone` 里的 `required: true`），已移除。

### 已测试

- 后端：`manage.py check` 通过；受影响 app（`purchasing`/`scheduling`）单独测试全绿，收尾时跑了一次全量 `manage.py test`：**235 项全部通过**（172.9秒）。新增迁移 `purchasing/migrations/0006_supplier_bank_account_furigana.py` 已在开发库上执行。
- 前端：`type-check`（`vue-tsc --build`）、`oxlint .`、`eslint . --no-cache`、`build-only` 全部通过。
- 真实浏览器操作验证：进货 suggestions 接口 curl 计时对比；排班格子右键反向循环（截图确认 休→全日 一步到位）；实际工作确认删除/标记缺勤全流程（含弹窗文案、保存后状态、随后手动改回原值清理测试痕迹）；月度经营打印区域 DOM 结构与模拟渲染高度检查；每日报表负责人下拉选项列表（DOM 级确认只含4名前厅员工，不含3名厨房员工）；进货录入保存徽标 DOM 检查（含文案与图标）；供应商假名字段保存后 API 回读确认、创建空电话供应商验证不再报错（测试数据已删除清理，不留痕迹）。

### 未做但用户提到、本轮刻意未处理的部分

用户在同一批反馈里还提到"最好可以去 GitHub 找一个成熟的打卡和工资计算方案，整体替换优化掉"排班/工资体系。本轮**没有**做整体重构或引入外部代码库，原因：
- 具体到"全天班点击太多次""无法删除记录"这两个可验证、可操作的痛点已经用上面第3项的方式解决了。
- "整体替换"是架构级改动，风险与工作量都远超本批次其余6项，且当前 `v2_simple` 工资引擎、多租户隔离、月度周期排班这些都是前几轮已经打磨过的既有设计，贸然替换有破坏现有测试覆盖和数据兼容性的风险。
- 如果之后确实要做，建议先单独立项，产出一份对比现有实现的具体重构提案（要解决什么问题、迁移路径、对已有 5000+ 条历史数据和已锁定工资结果的兼容处理），经用户确认后再动手，而不是在一次反馈修复批次里顺带做掉。

## 2026-08-19 本轮修复与重构

### 已完成

- 后端强制校验 Organization/branch/关联对象：管理员不能向其他 Organization 的 branch 写入；分店账号不能引用其他 Organization 的 supplier/employee/shift；日报负责人必须属于日报分店；普通 PATCH 不能移动业务记录的 branch。
- `ActualWorkRecord` 强制 employee/branch/shift/date/period 一致，身份字段不可普通 PATCH；非缺勤必须有实际起止时间，非跨夜结束必须晚于开始，休息分钟不能超过实际跨度。工资仍只读取 `manager_confirmed`/`admin_locked`，未增加迟到或晚走字段。
- 特殊计薪期间改动（包括清除）后立即重算，不再显示或锁定旧金额；默认月末按真实日历计算，离职当月默认截止 `leave_date`。
- `SchedulePeriod(branch, month)` 恢复 MySQL 普通唯一约束；`month=NULL` 历史行仍允许多条，并保留友好错误码 `schedule-period-month-already-exists`。
- 日报仅接受当前分店真实支付方式 ID；未知、已删除、其他分店/组织 ID 由 API 拒绝，现金及现金剩余均由后端计算；保留 183430 营业额回归用例。
- 进货 API 返回 `prior_month_avg_unit_price`、`price_direction`、`price_delta_amount`、`price_delta_percent`；严格只比较紧邻上个日历月。联想最近价由一次带 `Subquery` 的查询取得，消除逐品项 N+1。
- 新增 `StaffMember.work_area`（`kitchen`/`hall`）及数据迁移：按旧 `role` 尽量识别，无法识别默认 `hall`。迁移后原 7 人为厨房 2、前厅 5。
- 排班页面改为日期为行、员工为列，厨房/前厅上下两张独立表；取消用户可见的周期管理/手工起止日期。部分保存成功后只清理成功格子，失败或拒绝 override 的格子保留选择与 dirty 状态。
- 工资设置移动到员工新增/编辑；工资页只保留分店、月份、计算/更新、确认、打印和月度行内交通费/奖金。内部仍保留历史 `WageRule` 与 draft/confirmed/locked。
- 新增开发命令 `seed_schedule_wage_demo` 和租户开通命令 `provision_organization`。种子数据不创建新登录账号，也不修改原 4 个账号密码。
- 生产构建不再读取 `.env`/`.env.local` 中的 localhost API；本机值位于 `.env.development.local`，部署模板为 `.env.production.example`。

开发种子已写入心斋桥店（无新增账号）：

| 员工 | 区域 | 时薪 | 默认月交通费 | 奖金 | 2026-08 结果 |
| --- | --- | ---: | ---: | ---: | ---: |
| 排班演示・厨房A | 厨房 | ¥1,200 | ¥800 | ¥1,000 | ¥47,100 |
| 排班演示・厨房B | 厨房 | ¥1,250 | ¥900 | ¥1,500 | ¥50,525 |
| 排班演示・前厅A | 前厅 | ¥1,150 | ¥700 | ¥500 | ¥44,900 |
| 排班演示・前厅B | 前厅 | ¥1,300 | ¥1,000 | ¥2,000 | ¥52,400 |

共 24 条排班、24 条已确认实际出勤，含一次 10:45 实际到店和一次 22:30 实际离店；未创建迟到/晚走字段。原4个演示账号及密码保持不变，本轮未新增 staff 登录账号。

### 已测试

- 数据库结构修改前备份：`store-admin-backend/db_backups/store_admin_pre_20260819_followup.sql`。
- 迁移前：进货 5032、供应商 30、分店 3、账号 4、员工 7；迁移后上述数量完全一致。
- Django 完整测试：230 项通过；另外覆盖排班部分保存纯函数的 Node 测试 1 项通过。
- `manage.py check`、`makemigrations --check --dry-run`、`pip check` 通过；`check --deploy` 在本机开发配置下仅报告预期的 6 项生产安全提醒。
- 前端 type-check、production build、oxlint、eslint 全部通过；生产 dist 不包含 `http(s)://localhost` 或 `localhost:端口` API 地址；`npm audit --omit=dev --audit-level=high` 为 0 vulnerabilities。

### 仍需人工确认

- 按本轮要求没有使用浏览器、GUI 或截图。请人工确认两张横向排班表在真实员工数量下的滚动手感、工资打印、进货展开备注与涨跌辅助文字的视觉效果。
- 生产部署时必须由平台提供正式 API 地址和后端安全环境变量，并在 HTTPS 环境验证反向代理/CORS/CSRF。

### 已知限制

- `v2_simple` 不是法定工资申报系统，不计算税、社会保险、深夜/加班/法定休日加成或正式员工月薪；旧 v1 字段仅为历史兼容。
- 本次开发种子复用了数据库中原有的 2026-08 心斋桥“已确认但零结果”占位月结，在其为空时一次性填充 demo 结果并保持状态；若真实环境已有已锁定或非空已确认月结，命令会拒绝覆盖。
- 生产 JS 仍有 Vite 的大 chunk 警告（主包约 1.37 MB，gzip 约 448 KB），不影响本轮功能正确性，但后续可继续拆包。

## 项目结构

```
shop/
├── store-admin-frontend/   Vue 3 + TS 前端 —— 已完全接入真实后端，不再有 mock 层
├── store-admin-backend/    Django + MySQL 后端 —— 已完成，前端所有页面均对接此后端
└── design_mockup.html      设计稿来源（Apple 风格设计系统的 CSS 变量出处）
```

## 技术栈

- 前端：Vue 3.5（`<script setup>`）+ TypeScript + Vite + Element Plus 2.14 + Pinia（auth / preference / branches）+ vue-i18n 9（中/日）+ ECharts。
- 后端：Django 4.2 + DRF + MySQL（PyMySQL 驱动）+ `djangorestframework-simplejwt`（JWT 认证）。
- 报表导出：浏览器原生打印对话框直接另存为 PDF（`#print-area` + `@media print`），不再使用任何前端 Excel 库。

## 前后端对接架构

- **`src/api/*.ts` 是唯一的数据层**：每个文件对一个后端 app（`masterData`/`accounts`/`staff`/`suppliers`/`purchasing`/`dailyReport`/`dashboard`），内部做 snake_case ↔ camelCase 的 DTO 转换，后端数字主键统一 `String(id)` 化以保持前端组件原有的字符串 id 假设（`PaymentMethodDef.id` 除外，仍是 number，因为它只作为函数参数从不进 `v-model`）。`src/mock/` 目录已整个删除。
- **`src/api/http.ts`**：封装 `fetch`，自动带 `Authorization: Bearer`，401 时自动用 refresh token 静默重试一次；`ApiError.messages()` 统一拍平 DRF 两种错误形状（`ValidationError('msg')` 是裸数组 `["msg"]`，序列化器字段错误是 `{field: ["msg"]}`）。
- **JWT 会话持久化**：`stores/auth.ts` 的 `restoreSession()` 用存储的 token 换回登录态。`main.ts` 里必须先 `await` 完 `restoreSession()`/`preference.init()` **再执行 `app.use(router)`**——Vue Router 4 的首次导航守卫是在 `app.use(router)` 时触发的，不是 `app.mount()`；这个顺序踩过一次坑，导致每次刷新页面都把已登录用户弹回登录页。
- **支付方式的 `payment_amounts` 键**：不再用 `method.code`（分店内唯一，全局不唯一），改成 `String(method.id)`（数据库主键，全局唯一），前后端 `dailyreports/models.py` 的现金自动计算、`DailyReportForm.vue` 的非现金求和都统一按这个键对齐。

## 历史已完成功能（以下浏览器实测记录仅代表当时版本）

### 登录与权限
- JWT 登录 / 刷新 / 登出，`admin` 与 `branch` 两种角色，分店账号只能看到并操作本分店数据。
- 管理员强制重置任意账号密码（`POST /users/:id/reset_password/`）；任意账号自助改密（`POST /auth/change-password/`，需验证原密码）。

### 经营看板
- `GET /dashboard/summary/` 做真实聚合（今日/昨日营业额、客数、客单价、本月/上月进货额、14 天趋势、今日分店对比），按角色自动限定到本分店或全部分店。

### 每日报表
- `GET/POST/PATCH /daily-reports/`（当日"实时"报表，branch+date 唯一）与 `GET/POST /daily-report-history/`（每次保存的存档快照）是两个独立资源；保存动作先 upsert 实时报表，再追加一条历史快照，`edited_by`/`saved_at` 全部服务端强制赋值，不信任前端提交值。
- 支付方式明细、报销明细联想（`GET /daily-reports/expense_suggestions/`）均按分店隔离。
- 修改历史支持按营业日筛选、当日修改次数提醒、点击历史记录可编辑保存。
- 导出为 A4 单页 PDF，文件名格式"店铺-日期-日报表"。

### 进货管理
- `GET/POST/PATCH/DELETE /purchases/`，金额由后端计算（`quantity × unit_price`），前端不再传 `amount`。
- 品项联想改用真实接口 `GET /purchases/suggestions/`（原来前端本地打分排序的逻辑已移到后端）。
- 分店账号录入行自动绑定登录账号所属分店，不显示分店选择/分店列；管理员为纯查看视角。

### 供应商 / 员工
- 供应商 CRUD + 汇款账号 + 本月应付货款（自动按当月进货记录求和，可手动覆盖）；打印导出 PDF。
- 员工 CRUD，**已按分店隔离**（分店账号只能看到本分店员工，管理员能看到本组织内全部分店员工）；员工调店（跨分店转移）仅管理员可操作，详见下方"多租户与本轮新增功能"一节。

### 设置页
- 分店管理（增删改，删除时若还有账号挂在该分店会被后端拒绝并提示）。
- 账号管理（创建/编辑/重置密码/删除，不能删自己、不能删掉最后一个管理员）。
- 支付方式主数据按分店独立管理，新分店自动播种 8 项默认方式（现金/信用卡/PayPay/电子货币/积分/微信/大阪高级礼券/挂账），现金受保护不可改名删除。

## 后端（store-admin-backend/）

已完成的 app：`organizations`（Organization，多租户根）、`accounts`（User，role/branch/organization）、`branches`、`staff`（StaffMember + StaffTransfer）、`paymentmethods`（按分店隔离 + 新分店自动播种 + 拖动排序）、`dailyreports`（DailyReport + DailyReportHistory）、`purchasing`（Supplier + PurchaseRecord，含服务端筛选/分页/涨跌比较）、`scheduling`（月度自动周期排班）、`wages`（v2_simple 工资计算引擎）、`dashboard`（纯聚合视图，无模型）。全局分页仍关闭（`DEFAULT_PAGINATION_CLASS: None`），但 `/api/purchases/` 单独启用了 `PurchasePagination`（50条/页）——进货记录已增长到 5032 条，全量拉取不再实际可行；其余接口的数据量级仍然适合一次性返回，交给前端处理。

## 本轮迁移中发现并修复的问题

这些不是本轮引入的新 bug，而是从 mock 时代遗留、切到真实后端联调时才暴露出来的：

- `DailyReport.save()` 的现金自动计算原来硬编码字符串 `'cash'` 作为 key，切到按分店隔离的支付方式后这个 key 永远匹配不上，导致现金字段一直算错；已改成查询该分店 `protected=True` 的那条记录的真实数字 id。
- `BranchViewSet` 缺少"分店下还有账号时禁止删除"的服务端校验（mock 时代前端自己拦截，后端从来没补上）；已加上，并让前端 `deleteBranch()` 把 DRF 的 `["branch-has-accounts"]` 错误体转换成 `Error('branch-has-accounts')`，不然错误提示会静默失效（`ApiError.message` 永远是 `"API error 400"` 这种通用文案，不是后端返回的实际内容）。
- 顶栏"全 N 店"徽章原来无条件显示，分店账号登录后看到的也是"全 3 店"——已改成按角色区分：管理员看全部分店数，分店账号看自己所属分店的名字。
- 报表/账号/删除操作的成功提示里还留着 mock 时代的"（示例数据，未对接后端）"字样，已清空成正常提示文案。

## 真实数据导入记录

- 2023 年 1–2 月：从 `2023注文書.xlsm` 导入 766 条进货记录、26 个供应商，全部归入心斋桥店。
- 2026 年 1–8 月：从 `2026注文書.xlsm` 追加导入 4266 条进货记录，同样归入心斋桥店；供应商按名称去重合并，新增 4 个原表中没有的供应商（大庭水産/島之内青果/東陽商事/鑫福）。两份 xlsm 源文件都带有损坏的数据透视表（pivot cache 的 XML 里有 openpyxl 解析不了的属性），导入前都要先在文件层面剥离透视表相关部分才能正常读取，正文数据本身完好。
- 当前数据库合计：**5032 条进货记录**（2023-01-02 ～ 2026-08-16）、**30 个供应商**。

## 近期小修复（2026-08-18）

- 供应商下拉/列表排序改为按真实进货记录条数降序（`Supplier.objects.annotate(purchase_count=Count('purchase_records')).order_by('-purchase_count', 'name')`），不再按假名排序。
- 进货品项输入框的占位示例从"金枪鱼"（库存里从未出现过的品项）换成"空心菜"（实际有交易记录的品项）。
- 每日报表页的日期选择器默认值改为日本时间"今天"（`utils/format.ts:todayJst()`），不再受浏览器本地时区影响。
- 支付方式明细不再把日文/品牌原名（PayPay、微信、大阪premium商品券等）翻译成中文——中文用户对翻译后的名字反而更陌生，改成两语言都直接显示原名。

## 员工排班 / 实际出勤 / 时薪工资计算 / 月度经营分析（2026-08-18）

在不新建项目、不重复建模、不改动前 4 个演示账号密码、不触碰已有 5032 条进货记录的前提下，在同一套项目里新增了 `staff` 角色与完整的排班-出勤-工资-经营分析闭环。

### 新增角色：staff

- `accounts.User.role` 新增 `staff`，与 `staff.StaffMember` 建立可空、双向唯一的 `OneToOneField`（一个员工最多关联一个登录账号，一个账号最多关联一个员工）。
- 只有 `admin` 能创建/编辑/重置密码/删除 staff 账号（本阶段 branch 角色不具备该权限，设置页"账号管理"里已按角色隐藏对应按钮，后端也独立强制）；`staff` 账号只能自助改自己的密码。
- 项目级默认权限 `common/permissions.py:DenyStaffRole` 挂在 `REST_FRAMEWORK.DEFAULT_PERMISSION_CLASSES` 里，让**新写的和已有的每一个接口默认对 staff 角色 403**，只有显式加回 `permission_classes = [IsAuthenticated]` 的自助类接口（`/auth/me/`、`/auth/preference/`、`/auth/change-password/`、排班/出勤自助只读接口、自己的工资明细）才对 staff 放行——这是保证"staff 绝不能碰经营数据"这条硬约束不会因为漏改某个接口而破功的关键机制，`accounts/tests.py:StaffRolePermissionBoundaryTests` 对约 9 个代表性接口做了穿透式验证。

完整权限矩阵见 `store-admin-backend/README.md` 的"角色模型"一节。

### 排班模块（`scheduling` app）

- `SchedulePeriod`（排班开放周期）→ `AvailabilityRequest`（员工提交的可出勤意向）→ `Shift`（管理端排定的班次）。`Shift` 保存前跑 `scheduling/services.py:check_shift()`，硬冲突（同员工班次重叠）直接拒绝，软冲突（超出员工提交的可出勤范围）返回 `{'code': 'shift-conflict', 'warnings': [...]}`，前端弹二次确认后带 `override=true` 才能强制保存。`SchedulePeriod.publish` 之后班次转为只读发布状态。
- 跨午夜的时间段（比如 22:00–02:00 的班次）统一换算成分钟偏移量（`day_offset*1440 + hour*60 + minute`）比较，不会被当成"结束时间早于开始时间"误判。
- `ActualWorkRecord`（实际出勤确认）是管理端对"实际做了多久"的确认记录，**不是打卡机**——没有任何打卡、GPS、生物识别功能。可以从已发布的 `Shift` 用 `SchedulePeriod.generate_actual_records` 批量带出初始值，这个操作按 `(employee, work_date)` 唯一约束做了幂等处理，重复点击不会生成重复记录。

### 工资计算模块（`wages` app）

- 只有 `employment_type` 是 `hourly`（时薪）或 `temporary`（临时工）的员工才会被计算工资；`regular_monthly`（月薪正式员工）只统计出勤时长，永远不生成工资金额——这条规则被 `WageEndToEndFlowTests` 明确测试覆盖。
- 深夜时段 22:00–05:00（正确处理跨午夜切分）；加班采用"当日超8小时 + 当周超40小时"两级门槛，两者**不重复计算同一分钟**；同一分钟如果既是深夜又是加班，基本工资只算一次，深夜津贴和加班津贴分别叠加在上面。这套加班合并口径是**本工具内部的估算约定，不代表完全符合劳动基准法**——如果门店实际执行的是变形劳动时间制，需要先跟业主确认再决定是否调整这条规则。
- 工资规则按"实际出勤日期"落在哪条 `WageRule` 的 `effective_from`~`effective_to` 区间来选择，保存时用 `select_for_update()` 防止并发提交产生重叠规则。
- 法定假日津贴**只**在管理端明确勾选 `is_statutory_holiday` 时才计算，绝不根据日历自动推断。
- 交通费支持"每次出勤"或"每月固定（当月至少出勤1天才发）"两种口径。
- 手动调整金额（`manual_addition`/`manual_deduction`）必须填写 `adjustment_reason`，操作人自动记录，不能匿名调整。
- 舍入策略集中定义在 `wages/calculation.py`（`YEN_ROUNDING_POINT`/`YEN_ROUNDING_MODE`），当前是"四舍五入到1日元"，按日按项分别舍入。
- 如果某个已确认的出勤日找不到匹配的工资规则，月结确认会被直接拦下，返回缺失的员工+日期清单（`{'code': 'missing-wage-rule', 'missing': [...]}`），不会生成残缺或全零的结果。
- 每一份工资结果都带有法定声明："临时工／时薪员工工资计算资料，不包含所得税、住民税、社会保险、雇用保险等法定扣除，不属于最终工资申报结果。"（页面内、打印页都会显示）。
- 月结状态机：`草稿(draft) → 生成(可重复) → 确认(confirm，冻结结果，缺规则则拦下) → 锁定(lock，仅admin，version自增) → 解锁(unlock，仅admin，必须填写解锁原因，退回草稿)`，全部状态切换包在 `transaction.atomic()` + `select_for_update()` 里防并发。

### 月度经营汇总与自动分析（`dashboard/analysis.py` + 新页面）

- 新增独立页面（不是塞进现有经营看板），`GET /api/dashboard/monthly-analysis/?month=YYYY-MM&branch=<id>`：管理员不带 `branch` 参数=全链聚合（含分店对比图），带参数=单店；分店账号无论传什么 `branch` 参数都强制用自己的分店（不是报错拒绝，是静默纠正）；`staff` 角色 403。
- 营业额/客数只从实时的 `DailyReport` 取数，**从不**用 `DailyReportHistory`（那张表只用来算"当月修改次数"这个信号）。
- "暂定经营差额" = 营业额 − 进货 − 报销/支出 − 临时工/时薪工资。这个数字以及由它衍生的任何比率，在接口返回和页面上**永远不会**被称为"利润"/"毛利率"/"人工成本率"——`dashboard/tests.py:MonthlyAnalysisTerminologyTests` 直接断言返回体里不能出现这几个词（包括写在括号里的"仅供参考"式说明也不行）。
- 1月的"上月"正确解析为上一年12月；除以0（比如客单价在客数为0时）明确返回0，不会500；JSON 里所有金额字段都走 `Decimal(str(value))`，禁止用 float。
- 自动分析结论是**纯规则、非AI**：每条结论都带 `rule`/`threshold`/实际 `value`，措辞只用"建议确认"/"与通常数据差异较大"这类中性说法，绝不出现"舞弊"/"经营异常"这类词；样本不足（当月有日报天数 <5天）明确返回"数据不足，暂不判断"而不是勉强给结论。当前两条规则：单日营业额偏离月均 ≥40% 提醒确认；同一天历史记录被修改 ≥3 次提醒确认。阈值都是模块顶部的常量，方便以后调整。

### 前端新增页面

`SchedulingView.vue`（管理端排班：周期/班次/实际出勤三个标签页）、`WagesView.vue`（管理端工资：规则/月结两个标签页，含打印）、`MonthlyAnalysisView.vue`（月度经营：KPI卡+5个图表+自动分析+明细列表+打印，点击某一天可直接跳转到该分店该日期的每日报表）、`staff/MyAvailabilityView.vue`、`staff/MyShiftsView.vue`、`staff/MyWagesView.vue`（含打印）、`staff/MyPasswordView.vue`。路由按角色分组并在 `router/index.ts` 用 `meta.roles` 做前端级门禁（仅UX作用，不能替代后端权限），侧边栏 `AppSidebar.vue` 按角色显示不同导航。

### 测试与验证结果

- 后端自动化测试：**94 个全部通过**（`python manage.py test`，58.9秒），覆盖排班硬冲突/软冲突/跨午夜、工资规则重叠校验、工资计算引擎（含一条完整验证过金额的端到端流程：基本工资9600日元/总计10100日元）、staff 角色 9 个接口的权限穿透测试、月度经营的空数据/除零/1月边界/禁用词汇断言。
- 前端 `npm run type-check`（`vue-tsc --build`，项目实际用的类型检查脚本，比 `vue-tsc --noEmit` 更可靠——后者曾经漏掉过真实的类型错误）：0 错误。
- 前端 `npm run build-only`：构建成功。
- 前端 `npx eslint .`（不带 `--fix`）：0 错误。`npx oxlint .`（不带 `--fix`）：0 错误。
- 真实接口联调（对着本机运行中的 Django 服务用 curl 实测，非单纯读代码）：4个演示账号登录、月度经营的全链/单店/分店越权纠正/1月边界/极端空月份、staff 角色实时创建-登录-验证9类接口403/200边界-清理，未认证请求拒绝，共 26 项断言全部通过；用于测试的临时 staff 账号测试后已删除，4个正式账号数、5032条进货记录、3个分店在测试前后数量不变。
- 打印/PDF 视觉效果（新增的工资单打印、月度经营汇总打印，以及 ECharts 图表渲染完成后再触发打印这几点）**无法在无头环境里可靠确认**，需要用户在浏览器里手动打开 `/wages`、`/my-wages`、`/monthly-analysis` 三个页面实际点一次"打印"预览确认排版正常。

## 多租户改造与工资/排班/进货/支付方式全面升级（2026-08-19）

在不新建项目、不重复建模、不删除迁移、不损坏已有 5032 条进货记录、不改动 4 个演示账号密码的前提下，完成了一轮更大的改造：引入 Organization（店铺集团）多租户层，重做员工调店、月度自动排班、简化版工资引擎（v2_simple）、进货筛选/涨跌比价、支付方式拖动排序。迁移前已用 `mysqldump` 生成真实备份（`db_backups/store_admin_pre_org_migration_20260818_205608.sql`）。

### Organization 多租户层与权限结构

- 新增 `organizations.Organization` 模型（`code`/`name_zh`/`name_ja`）。`User`/`Branch`/`Supplier` 各自加上 `organization` FK（`on_delete=PROTECT`）。
- 迁移路径严格按"可空 FK → 数据迁移回填 → 收紧为非空"的兼容顺序执行，每个 app 单独走完整三步（`accounts`：0003→0004，`branches`：0002→0003，`purchasing`：0002→0003），从未一步到位地直接加非空约束。
- 数据迁移 `organizations/0002_backfill_default_organization.py` 把所有既有数据（3 个分店、4 个账号、30 个供应商、5032 条进货记录等）统一归入自动创建的默认组织 `default-store-group`（现有店铺集团／既存店舗グループ），迁移前后所有既有数据的可见性、可操作性均未发生变化。
- 所有业务查询集都**显式**按 `organization_id` 过滤——不依赖"反正都是通过 branch 间接属于某个组织"这种隐式假设。共享的 `common/permissions.py:BranchScopedQuerysetMixin` 扩展为同时按组织与分店两级过滤，一次改动正确传播到 4 个以上的 ViewSet。
- 管理员不再能看到系统里的其它组织——这是本轮修复的一处真实缺口：改造前"admin"隐含"看到全部"，多租户后这个假设不再成立。
- **发现并修复一处真实安全漏洞**：原 `admin` 账号的 Django 原生 `is_staff`/`is_superuser` 被旧播种脚本设为 `True`，这会让业务层的组织隔离对 Django 自带的 `/admin/` 后台完全失效（`/admin/` 从不感知 Organization）。数据迁移 `accounts/0005_separate_business_admin_from_django_superuser.py` 清除了所有 `role='admin'` 行的这两个标志位；`seed_demo_data` 命令也已同步修正，4 个演示账号的密码哈希和登录行为完全不受影响（已用 curl 逐一验证）。
- 新增 A/B 双组织隔离测试基类 `common/test_utils.py:TwoOrganizationApiTestCase` 和 `organizations/tests.py:OrganizationIsolationTests`（22 项断言，覆盖每日报表/进货/排班/工资/供应商/员工/支付方式/月度经营的跨组织隔离，以及管理员越权访问另一组织分店参数的场景），全部通过。

### 员工调店（`staff.StaffTransfer`）

- 仅管理员可操作，仅限同一组织内，`POST /api/staff-transfers/` 原子完成"改 `StaffMember.branch` + 同步关联登录账号的 `branch`"两步。
- 若该员工在旧分店有转移生效日之后的未来班次，默认拒绝并返回 `{'code': 'has-future-shifts-at-old-branch', 'shifts': [...]}`，需要二次确认（`force: true`）才会强制转移——绝不静默丢弃这些班次。
- 历史记录（过往班次、过往工资结果）永远保留原分店归属；只有员工的"当前分店"会变。反复调店（含调回原分店）每次都新建一条 `StaffTransfer` 记录，历史记录本身永不覆盖或编辑。
- 前端在员工管理页新增调店对话框与调店历史查看。

### 排班改为月度自动周期

- 新增 `BranchScheduleSetting`（分店级默认班次时间模板：上午/下午/全日各自的起止时间），`SchedulePeriod` 改为只需"分店+月份"即可创建，起止日期一律服务端按日历月计算，月份创建后不可更改。
- 前端排班页整体重做为电子表格式月度大表（员工×日期，格子状态 休/上午/下午/全日），支持范围批量填、复制上月、复制某员工整月、清空某员工整月，草稿/发布两阶段工作流（未保存改动有明显提示，发布前二次确认），移动端卡片视图，每日出勤人数统计栏（全日班次不重复计入两次）。
- staff 端"我的可出勤"页同步改为按月选择的范式。
- 发现 MySQL 不支持带条件的唯一约束（`models.UniqueConstraint(..., condition=Q(...))` 会产生 `models.W036` 检查警告且在真实数据库里完全不生效）——已移除这条误导性的声明式约束，唯一性检查完全放在序列化器层面执行，并新增迁移 `0005_remove_scheduleperiod_unique_schedule_period_per_branch_month` 干净地去掉这条从未真正生效过的约束声明。

### 实际工作确认简化

- 明确不是打卡系统，不单独记录迟到/早退/延长分钟数。
- 新增校验：实际时间与关联班次不一致时，必须填写 `adjustment_reason`，否则拒绝保存。

### v2_simple 工资计算引擎（全新版本，绝不覆盖/重算 v1 已锁定结果）

- 公式极度简化：`基本工资 = 实际计薪分钟 / 60 × 时薪`，**不计算深夜/加班/法定假日津贴**——这是本工具内部的刻意简化，页面与打印页都明确标注"不包含所得税、住民税、社会保险、雇用保险等法定扣除，不属于最终工资申报结果"，不是声称完全符合劳动基准法。
- 只有 `hourly`/`temporary` 员工会被计算工资金额；`regular_monthly` 员工仍然只统计工时。
- 工资规则表单简化为仅：员工／生效起止日期／时薪／默认交通费／备注（v1 时代的深夜/加班/法定假日加成比例字段仍留在数据模型里，供历史数据使用，新页面不再展示或编辑）。
- 交通费支持按月覆盖（`monthly_transportation_override`，为空则回退到规则默认值 `rule_transportation_amount`，清空即时生效不需要重新生成），覆盖需填写原因，操作人与时间自动记录。
- 新增每月奖金（`bonus_amount`，独立字段，不滚存到下月，非零时必须填写说明，操作人与时间自动记录）。
- 新增离职员工特殊计薪期间：默认在 `leave_date` 落在月中时自动截止到离职日，也可由管理员手动指定，但必须落在本次月结所属月份范围内。
- 修复了两处真实的生产 bug（均由本轮新写的测试直接发现）：单独 PATCH `bonus_amount` 或 `monthly_transportation_override` 之前不会立即重算 `transportation_amount`/`estimated_total`，导致页面显示的金额是过期的——已在 `WageEmployeeResultViewSet.perform_update()` 里补上即时重算。
- 简化版逐日明细只显示：日期／实际到店时间／实际离店时间／实际休息分钟／实际计薪分钟／当日基本工资，去掉了深夜/加班/法定假日列。
- v1 时代锁定的历史月份继续使用当时冻结的 `estimated_total`，绝不重新计算或覆盖——28 项既有 v1 工资测试在新引擎上原样通过，证实了这一点。

### 进货管理：服务端筛选/分页/商品名称标准化/涨跌比价

- `GET /api/purchases/` 改为服务端分页（每页 50 条，可调至 200），支持 `branch`/`supplier`/`month`/`date_from`/`date_to`/`item_name`/`ordering` 筛选参数——原先一次性拉取全部记录的做法在数据量涨到 5032 条后已不现实。
- 新增 `PurchaseRecord.item_name_normalized` 字段（Unicode NFKC 归一化 + 空白折叠 + 大小写归一），已为全部 5032 条既有记录完成回填迁移（`0005_backfill_item_name_normalized.py`，回填后核实无遗漏空值）。刻意保守：不去除数量单位（"1个"/"1箱"）或品项名里附带的供应商后缀，避免把不同规格的商品错误合并。
- 月度涨跌比较：`price_change=up|down` 筛选（必须同时指定 `month`，避免对全表做无边界扫描），比较基准严格是"同（分店、供应商、标准化品项名）在紧邻上一个日历月的平均单价"——没有上月数据时绝不标记涨跌，绝不用陈旧的历史价格或跨年数据当基准，也绝不混用不同供应商的价格。
- 新增 `GET /api/purchases/price_history/` 历史价格抽屉（最近100条）与 `GET /api/purchases/supplier_comparison/` 供应商比价（同一品项跨供应商比价，这是唯一一处刻意允许"混合供应商"的功能，因为比价本身就是目的）。

### 支付方式拖动排序

- 新增 `POST /api/payment-methods/reorder/` 原子批量重排序端点：一次性传入某分店的完整 id 顺序列表，服务端校验必须恰好等于该分店现有的全部 id（不多不少、不重复），否则拒绝——避免部分重排序导致排序值错乱。
- 现金仍然不可改名/删除，但**可以**参与拖动排序（修复了 `PaymentMethodDefSerializer.validate()` 原先连 `sort_order` 都一并拒绝的过度限制）。
- 前端设置页改为原生拖放（无额外依赖库）的可拖动列表。

### 完整新增/变更 API 列表

```
POST   /api/staff-transfers/                          员工调店
GET    /api/staff-transfers/                           调店历史查询
GET    /api/branch-schedule-settings/{branch_id}/       分店班次时间模板
PATCH  /api/branch-schedule-settings/{branch_id}/
PATCH  /api/wage-employee-results/{id}/                 奖金/交通费覆盖/计薪期间
GET    /api/purchases/?month=&branch=&supplier=&        进货列表（分页+筛选+排序）
       item_name=&price_change=&ordering=&page=
GET    /api/purchases/price_history/                    历史价格
GET    /api/purchases/supplier_comparison/               供应商比价
POST   /api/payment-methods/reorder/                     支付方式批量重排序
```

（`schedule-periods`/`availability-requests`/`shifts`/`actual-work-records`/`wage-rules`/`wage-monthly-closings` 等既有端点的请求/响应结构本轮也有调整，具体字段见 `store-admin-backend/README.md`。）

### 测试与验证结果

- 后端自动化测试：**213 项全部通过**（本轮最后一次完整 `python manage.py test` 运行，158秒，覆盖多租户隔离/员工调店/月度排班/v2_simple 工资/进货筛选与涨跌比价/支付方式重排序等全部新增功能，以及全部既有回归测试）。
- 迁移：`python manage.py makemigrations --check --dry-run` 无待生成迁移；`showmigrations` 确认全部迁移已应用；迁移前后关键数据量核对一致：Organization 1 个、分店 3 个、账号 4 个、供应商 30 个、进货记录 5032 条（`item_name_normalized` 回填后无空值）。
- 4 个演示账号密码全部验证可正常登录（含 JWT 刷新令牌流程、CORS 预检）。
- 前端 `npm run type-check`（`vue-tsc --build`）：0 错误。`npx oxlint .`（不带 `--fix`）：0 错误。`npx eslint . --no-cache`（不带 `--fix`）：0 错误。`npm run build-only`：构建成功。
- `python manage.py check --deploy`：6 项提示，全部是本机开发环境固有的部署前安全提醒（HSTS/SSL重定向/SECRET_KEY强度/Cookie安全标志/DEBUG=True），不是代码缺陷，生产部署时需按提示逐项配置。
- 真实接口联调：对本机运行中的 Django 服务用 curl 实测（非仅读代码）进货筛选/分页/涨跌比价/历史价格/供应商比价、支付方式批量重排序（含把现金拖到末尾再复原）、工资月结列表结构，均返回预期结构与数值。
- **打印页面视觉效果无法在无头环境里可靠确认**，需要用户在浏览器里手动打开 `/wages`、`/my-wages` 实际预览一次简化后的打印排版；同样，支付方式拖放交互的实际手感（HTML5 原生 drag-and-drop 在不同浏览器下的视觉反馈）、进货页价格历史/供应商比价抽屉的展示效果，建议用户实际操作一次确认。

## 已知限制

- 供应商信息里没有汇款账号/类别/联系人/支付方式，这些字段是空的（无论是 2023 还是 2026 批次的真实数据，原始 Excel 里都没有这些维度）。
- "本月应付货款"这类当月口径统计，只有分店账号用当前日期录入新数据后才会非零——历史导入数据是 2023 年的，不计入"本月"，这是预期行为。
- v2_simple 工资计算结果不包含个税/住民税/社会保险/雇用保险等法定扣除，不能直接当最终工资单使用（每份结果和打印页都带有法定声明，见下方"多租户与本轮新增功能"一节）。
- v1 时代按"日超8h+周超40h"估算的加班/深夜/法定假日津贴口径仍保留在历史已锁定月份里（永不重算），但**新生成的月份不再计算这些津贴**——v2_simple 只按实际计薪分钟 × 时薪计算基本工资，这是本轮刻意简化，不是遗留问题。
- 工资/排班/进货比价的打印排版效果、拖动排序的实际手感未经人工视觉确认（无头环境无法可靠验证，见下方"测试与验证结果"）。
- 分店账号本阶段仍不能创建/管理 staff 账号、不能操作员工调店（设计如此，只有 admin 可以）。
- 进货品项名称标准化只做了全角/半角与空白字符的归一化，不会合并数量单位不同的同名商品（如"1个"与"1箱"），也不会去除品项名里附带的供应商后缀——这是刻意保守的设计，避免把不同规格的商品错误合并进同一条价格趋势。

## 演示账号密码

密码不在此公开仓库里明文列出，见 `branches/management/commands/seed_demo_data.py`（本地跑一次即可用这些账号登录）。

| 账号 | 角色 | 分店 |
| --- | --- | --- |
| `admin` | 管理员 | 全部（本组织内） |
| `shinsaibashi01` | 分店账号 | 心斋桥店 |
| `namba01` | 分店账号 | 难波店 |
| `umeda01` | 分店账号 | 梅田店 |

现在前后端只有**一套**真实凭据（存在 MySQL 里，密码经 Django `set_password()` 哈希），不再有"前端 mock 一套、后端 seed 一套"的两份独立账号。以上账号已逐一登录验证：正常登录、错误密码被拒绝、管理员强制重置密码、分店账号自助改密，全部走真实 JWT 认证链路测通。
