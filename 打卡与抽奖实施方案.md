# 打卡与抽奖实施方案

- **状态**：设计稿，待阶段 0 拍板
- **栈**：Django 4.2 · DRF · MySQL · Vue 3 · Element Plus
- **新增设备**：柜台平板 + 蓝牙读码器
- **定位**：一套轻量店铺积分卡——客人凭卡片二维码打卡、按消费额攒积分、用积分抽奖或换券。明着收手机号、不做手机验证、不接 LINE、不做会员等级。
- 本稿整理自 2026 年 8 月关于评审稿（`抽奖与打卡功能方案.md`）的多轮讨论，替代评审稿第 3–10 章中身份、打卡、二维码、店员流程的相关部分；数据模型与反作弊在评审稿基础上补充积分、里程碑、奖池分档与券。

---

## 1. 概述与结论

**整体可行，与现有架构契合度高。** 方案对技术栈和多租户模型的判断准确，核心设计（服务端抽奖、店员为消费信息唯一来源、追加不可删、Organization / Branch 隔离）几乎都能直接复用代码库里已经存在的模式。难点在工作量和细节完整度，不在"能不能做"。

### 第一版闭环

客人首次到店留手机号（+ 选填姓名、生日）拿一张卡片二维码 → 每次结账时把卡片凑到柜台读码器、店员输金额、点确认 → 这一下同时记打卡 + 按消费额发积分 → 客人走人后用积分抽奖或换下次抵用券 → 抽奖奖品也都是下次到店券 → 下次到店店员扫券核销。

### 三条贯穿全案的规则

1. **唯一可信事件**是"店员在客人面前点的那一下确认"。客人端能自助做的一切（登记、扫码、抽奖、兑积分）在被店员确认之前都不产生价值。
2. **价值一律"下次用"的券**（抽奖奖品、积分兑换、里程碑奖励）。客人不在收银台抽奖 / 不看结果，收银台只发积分、不碰账、不退款、不当场抵扣。
3. **不做手机验证**。客人用别人的号也行——防作弊靠店员确认的那笔真实消费，跟号码真假无关，送的东西损失有限（见 §14）。

### 项目归属

整个功能在 `shop` 项目内：

- 后端：`store-admin-backend` 里新增 `promotions` app
- 前端：现有 Vue SPA 的三块——**管理后台**（活动 / 奖品 / 概率 / 库存 / 客户信息，沿用现有 JWT + 角色）、**店员平板页**（一个 `staff` 路由 + kiosk 模式）、**公开客人页**（public 路由）
- 一套代码、一套部署。柜台平板只是一台指向 staff 路由的设备，不是独立项目
- 中奖内容、概率、库存、客户信息的管理**全部在现有 shop 管理后台**

### 与评审稿的差异

评审稿假设"打卡 = 自助扫码 + 匿名会话"，并配了 60 秒轮换二维码 + 定位 + Turnstile 作为防刷。本稿把防刷的重量整体移到"店员结账时点确认"这一步，因此**不需要轮换二维码屏幕、不需要定位、不需要人机验证**。评审稿的"客人不注册 / 匿名"不是本方案目标——这里就是一套明着收手机号的店铺积分卡；LINE、动态二维码、定位、Turnstile 全部不做。

---

## 2. 核心原则：信任模型

设计前提是两个极端假设：

| 角色 | 假设 | 推论 |
|---|---|---|
| **客人** | 全部是想刷奖的坏人：会改网页、调接口、伪造请求、拍照转发二维码、换设备换号码 | 客人端任何输入都当成假的。客人自助能触发的动作绝不直接产生打卡、积分或奖品 |
| **店员** | 诚实、在场、看得到桌子和账单 | 店员登录后点的"确认"是系统里唯一被信任的事实来源。打卡和积分都挂在这一步上 |

在这个模型下，评审稿里大量客人端防刷措施（动态令牌、定位、设备指纹、Turnstile）都不再是第一版的必需品——因为客人端本来就产生不了价值。防护重心变成 **店员权限最小化 + 记录不可删 + 异常可追溯**。

> **这个模型不覆盖什么**
> "店员诚实"是一个前提，不是系统保证。店员与客人串通、把实际 ¥3,000 录成 ¥10,000、拿一笔结账替多个客人确认——这些技术上挡不住，只能靠审计（见 §13、§14）。POS 不提供交易号时，这是必须书面接受的残余风险。

---

## 3. 客人识别：手机号 + 卡片二维码

这就是一套**店铺积分卡**：客人第一次留手机号，拿一张卡（二维码），以后每次出示。不追求匿名，不做手机验证，不接 LINE。

### 怎么用

| 场景 | 做什么 |
|---|---|
| **第一次到店** | 扫柜台 / 桌上二维码 → 页面「留个电话开始集章」→ 填**手机号**（必填）+ **姓名、生日月日**（选填）→ 拿到卡片二维码。同时勾一句隐私告知 |
| **每次到店** | 结账时卡片二维码凑到柜台蓝牙读码器 → 平板显示「田中さん / 积分 340 / 集章 3/5」→ 店员输金额 → 确认 |
| **换设备 / 只想看** | 手机号 + 生日月日「只读登录」：能看卡、积分余额、流水、券；不能核销 / 花积分远程套现 |
| **丢手机 / 清浏览器** | 店员在平板上按**手机号**查回；客人报错号或懒得报，就当新客重新登记（积分和券跟着丢，可接受） |

### 卡片二维码存在哪

- 客人手机浏览器里存一份（`Set-Cookie` HttpOnly + localStorage 镜像，每次到店滚动续期），下次打开页面自动认出，不用重新报手机号
- 服务端按手机号建一条客人记录，卡片二维码里放一个稳定 token 关联这条记录
- 浏览器那份丢了不影响——手机号是主键，店员随时查得回

### 不做手机验证 = 可接受的取舍

- 客人填假号、填别人的号都行。反正他人到了店、消费了，送的东西损失有限
- 防作弊靠"店员结账时确认的那笔真实消费"，与号码真假无关（§2）
- 代价：客户库里会混入假号 / 重复登记，做生日营销、统计会员数时打折——已接受（§14）

### 不做的

- **手机 / 短信验证码** —— 不需要短信服务、不增加到店摩擦。副作用（假号、账户可被窥）已接受，见 §14
- **LINE 官方账号 / LIFF** —— 账号已与他人绑定、要账号密码，太麻烦，完全不碰
- **会员等级 / 会员价** —— 只做单层积分卡。等级会逼出定向推送、分层引擎、会员价逻辑，收益不值

---

## 4. 打卡与二维码

### 一次到店只有一个"记账点"：结账时读客人的卡片二维码

结账时客人把卡片二维码凑到柜台读码器 → 店员输金额 → 回车。这一下同时产生：

- 今天的打卡（+ 集章 +1，如果启用集章）
- 按消费额发积分（¥1,000 → 10 分，见 §5；没有活动时也可跳过金额，只"确认到店"记打卡）

### 两种二维码

- **店内固定二维码**（印刷贴纸，桌上和柜台都贴，永不更换）：只是**登记入口**，新客人扫它去填手机号；老客人扫它打开自己的卡片页。里面只放一个短 token（关联分店 + 活动），**不放整条网址**
- **客人的卡片二维码**：登记后生成，稳定不变，存在客人手机浏览器里。结账时给读码器扫的是这个。里面是关联客人记录的 token
- 兜底：读码器读不出时，卡片页也显示一串短码，店员可手输；再不行按手机号查

> **桌上也贴固定二维码的作用**
> 老客人吃饭时先扫一下打开自己的卡片页，结账时卡片二维码已经在手机上，不用现找。桌面码只是让结账更快，不是第二个记账点。

### 不做（评审稿里有、本稿降级为将来可选）

- 60 秒轮换二维码 + 展示屏幕 —— 需要每店一台联网设备，本稿用店员确认替代其防刷作用
- 浏览器定位 —— 零门槛可伪造、要权限弹窗、要 HTTPS、市区室内精度 50–200 米分不清相邻店铺
- Cloudflare Turnstile —— 客人端已产生不了价值，无需人机验证

---

## 5. 积分、里程碑与集章

积分是主货币：店员结账确认金额那一下，按消费额发积分，同时打卡 +1。积分能花在三个地方——抽奖、下次抵用券、里程碑。两条主路（抽奖 / 换券）对应两种客人：**爱玩的赌大的，保守的慢慢攒**。

### 比例（阶段 0 起点，跟奖品成本一起校准）

| 项 | 值 | 说明 |
|---|---|---|
| **赚分** | ¥1,000 → 10 分 | 1%，日本正常返点水平 |
| **花分 · 抽奖** | 100 分 → 1 次 | ≈ ¥10,000 消费换一抽 |
| **花分 · 抵用券** | 100 分 → ¥100 券 | 下次到店抵用（不当场抵，见 §7） |
| **里程碑** | 累计 300 / 800 / 2500 分 | 额外送 饮料券 / 甜品券 / ¥500 券，叠在 1% 之上，给常客加码 |
| **积分过期** | 末次活动 + 12 个月 | 日本惯例；需定时清理任务 |

> **两条路的价值要接近**
> 100 分换 ¥100 券 或 换 1 抽，那么**一次抽奖的平均回报应在 ¥100 上下**（略低 10–20% 用来定价"乐趣"）。配成平均一抽只值 ¥40，爱玩的客人算出来就跑；配成 ¥120，你在赌徒身上亏钱。让心理区分两条路，别让数学区分。奖池 EV 见 §6。

### 积分流水账

- `PointsLedger`：每一笔赚 / 花 / 过期都是一条不可变记录（像 `inventory.StockTransaction`），`Customer.points_balance` 是它的汇总
- 店员平板和后台都能看这条客人的流水，用于回答"我积分怎么少了"
- 花分（抽奖 / 换券）在事务内扣余额 + 写流水 + 生成券，行锁防并发

### 集章（按到店次数，可选）

积分奖励"消费多"，集章奖励"来得勤"——两个不同的轴。集章 = 每次店员确认 +1 个章，满 N 次给一个小奖（如一杯饮料或一笔 bonus 积分），然后清零重来。给高频低额的客人一个理由。

> **阶段 0 要定**：要不要保留独立的"按次数集章"，还是只按积分（=只奖励高消费）？保留的话，集章满几次、送什么、清零还是循环。

### 合规

- 积分从消费产生、只在自家店用 → 算**值引き（折扣）**，常见做法
- **不可用现金购买积分**——否则触发前払式支払手段登记
- 抽奖奖品（景品）价值受景品表示法对交易额的比例约束，配奖池时留意

---

## 6. 抽奖与奖池

### 抽奖次数从哪来

- 主路：客人用**积分兑换**（100 分 / 次，见 §5）
- 可选双轨：当次消费达门槛（如 ¥1,000）直接送 1 次，给"即时感"——阶段 0 决定要不要
- 活动可设每次确认上限、每位客人每日上限
- 抽奖动作在**客人自己手机**上完成，收银台不看结果、不改账

### 抽奖必须由服务端完成

- 前端只播动画和结果；奖品选择在后端用安全随机数（`secrets` / `SystemRandom`）
- 事务内 `select_for_update()` 锁定积分余额 / 抽奖次数和奖品库存——范本 `inventory.services.adjust_stock`
- 幂等 `request_id` 防连续点击重复抽奖、重复中奖
- 抽奖记录保存当时的**权重快照**，后续调概率不改变历史结果
- "谢谢参与"也生成一条记录（返 30 积分），避免重复请求
- 所有奖品都是**下次到店券**，永不作用于当前账单（收银台不碰账、不退款）

### 奖池（权重总和 500）

概率由权重算出，服务端按启用奖品实时计算，抽中时存快照。

| 奖项 | 奖品 | 感知价值 | 店铺成本 | 权重 | 概率 | 每店每日 |
|---|---|---:|---:|---:|---:|---:|
| 终极大奖 | ¥5,000 优惠券 | ¥5,000 | ¥5,000 | 1 | 0.2% | 1 |
| 二等奖 | ¥1,000 优惠券 | ¥1,000 | ¥1,000 | 5 | 1.0% | 3 |
| 三等奖 | ¥500 优惠券 | ¥500 | ¥500 | 14 | 2.8% | 10 |
| 主厨指定料理 | 本日主厨指定 1 份 | ~¥1,000 | ~¥350 | 15 | 3.0% | 5 |
| 送菜（小菜） | 指定小菜 1 份 | ¥500 | ~¥180 | 45 | 9.0% | 不限 |
| 甜品 | 甜品 1 份 | ¥600 | ~¥220 | 85 | 17% | 不限 |
| 饮料 | 软饮 1 杯 | ¥400 | ~¥120 | 135 | 27% | 不限 |
| 谢谢参与 | 返 30 积分 | ¥30 | ~¥3 | 200 | 40% | — |
| **合计** | | | | **500** | **100%** | |

中"实物 / 券"概率 60%，"返积分"40%——不设"什么都没有"，输了也返 30 分。

### 成本核对（对上"100 分 = ¥100 券"这条线）

| 项 | 每抽 |
|---|---:|
| 客人感知期望价值 | ≈ ¥331 |
| 店铺账面期望成本 | ≈ ¥132 |
| 店铺实际期望成本（按券核销率 60% 估） | ≈ ¥82 |
| 对照：直接给 ¥100 券（60% 核销） | ≈ ¥60 |

抽奖比直接给券每抽多花 ≈ ¥18，这是买"更好玩、更粘"的钱。核销率是假设值，上线后用真实数据校准。

### 三个调节杆

- **想更省成本** → 饮料权重 135→90，谢谢参与 200→260；实际成本降到 ≈ ¥60，和直接给券持平
- **想更热闹** → 终极大奖每日库存 1→2，或砍掉谢谢参与改"保底饮料"
- **大奖月度预算超了** → 面值 ¥5,000→¥3,000，或库存改"每周 1 张 / 店"

> **主厨指定料理**
> 配置 `{ label: "本日主厨指定料理", menu_value_cap_yen: 1200 }`。券面："可兑换【本日主厨指定料理】1 份（菜单价 ¥1,200 以内）"。**兑换时以当天的主厨指定为准**，券永远新鲜。成本靠"菜单价上限 + 厨房挑毛利合适的菜（时令 / 余料）"控制。也给厨房一个每天推一道菜的用途，让奖池天天有变化。

> **别忘了跟里程碑 / 集章一起算**
> §5 的里程碑也送饮料 / 甜品，两个渠道叠加，免费饮料甜品月出货量不小。阶段 0 用 `预估月到店数 × 抽奖率 × 各档概率` 算月度总赠送成本，跟毛利对一下。

---

## 7. 券规则与兑换

券始终挂在客人记录（手机号）上。"怎么证明券是真的"和"怎么找回"是同一件事：**查这个客人有没有这张未用的券**。领券时券自动记到客人记录、卡片页出现；不需要客人保存任何东西。

### 没有开放式免单——顶奖是封顶券

"整单免单"对大额账单是无上限风险，而且如果留到下次用，客人会**报复性点单**。解法不是按金额调概率，是套硬上限：

- 顶奖 = **¥5,000 优惠券**。普通晚餐 ¥2,000–3,000，对多数人这就是免单，营销照喊；店铺最大损失锁死 ¥5,000
- 顶奖 `daily_stock = 1 / 店` → 每店每日免单敞口 = 一张封顶券
- 高价值券**需消费满 ¥X 才能用** → 杀掉"精确点到券面值、一分不付"的报复性点单
- 减免券（¥500 / ¥1,000）没有报复性点单问题——¥500 就是 ¥500

### 每种券的规则

| 属性 | 饮料 / 甜品 / 送菜 / 主厨指定 | ¥500 券 | ¥1,000 券 | ¥5,000 券 |
|---|---|---|---|---|
| 有效期 | 30 天 | 45 天 | 60 天 | 90 天 |
| 使用门槛 | 无（堂食即可） | 满 ¥1,500 | 满 ¥3,000 | 满 ¥6,000 |
| 店长确认 | 否 | 否 | 否 | **是** |
| 每店每日出奖上限 | 不限（主厨指定 5） | 10 | 3 | 1 |

通用：不可叠加、不找零、仅堂食、绑客人记录、每次到店限用 1 张、单次核销。

**店铺每店每日理论最大敞口** ≈ 1×¥5,000 + 3×¥1,000 + 10×¥500 + 实物券 ≈ ¥13,000（几乎不可能全中，实际远低于此）。

### 用券流程（下次到店，可能几周后）

客人卡片二维码给读码器扫 / 报手机号 → 店员平板上看到「田中さん · ¥500 券 · 有效期至 X · 未用」→ 点确认核销 → 在现有 POS 里手动应用折扣。系统校验：属于本店？未兑换？未过期？满足使用门槛？顶奖需店长二次确认。

> **代价**
> 券绑在手机号上（不验证），谁报出这个号 + 出示卡片，店员就发。蓄意冒用别人的号来领券是可能的，但价值有限（单张、有效期、使用门槛、顶奖店长确认），已接受。

---

## 8. 完整流程

### A0 · 新客人首次登记（只做一次）

1. **客人** 扫桌上 / 柜台的店内固定二维码，页面打开
2. **客人** 填手机号（必填）+ 姓名、生日月日（选填），勾隐私告知
3. **系统** 按手机号建客人记录 → 生成卡片二维码 → 存进浏览器（cookie + localStorage）

### A · 每次到店：打卡 + 拿积分

1. **客人** 吃饭时打开卡片页（浏览器已记住），或结账时直接出示卡片二维码
2. **店员** 结账时用柜台读码器读客人卡片二维码（读不出则手输短码 / 手机号）→ 平板显示客人名 + 积分 + 集章进度 → 输金额 → 点确认
3. **系统** 记为今天的打卡 + 按消费额发积分（+ 集章 +1）+ 跑风险规则
4. **店员** 平板回显：打卡已记 / +N 积分 / 风险：正常。客人正常付款离店

### B · 客人用积分（走人后，自己手机）

1. **客人** 打开卡片页，看到积分余额，选择：换抽奖次数 / 换 ¥100 抵用券
2. **系统** 事务内扣积分 + 写流水；抽奖再：检查活动 / 风险 → 锁奖品库存 → 按权重抽取 → 存记录（含权重快照）
3. **客人** 看到结果 / 券。券自动记到客人记录，卡片页出现这张券 + 有效期

### C · 下次到店：兑换券（里程碑 / 抽奖 / 积分换的券）

1. **客人** 出示卡片二维码，或报手机号
2. **店员** 平板上看到该客人名下的券 + 内容 + 有效期
3. **系统** 校验：属于本店？未兑换？未过期？高价值奖品是否需店长确认？
4. **店员** 点确认核销 → 在现有 POS 里手动应用免单 / 折扣 / 送菜

### 后台（admin / branch，现有 Web App 加菜单）

- 营销活动：活动 CRUD（积分比例、兑换比例、里程碑、上限、有效期、启用 / 暂停）
- 奖品管理：分档、名称、权重、库存（总量 / 每日）、券有效天数、使用门槛、是否需店长确认
- 记录：客人（含积分流水）、打卡、消费确认、抽奖、兑换、风险事件
- 操作：作废消费确认（填原因，撤销积分）、调整积分（留原因）、审核风险事件、审批高价值兑换、响应客人删除请求
- 二维码：只需打印固定贴纸，不需要轮换展示页

---

## 9. 硬件清单

关键点：设备**不需要靠近、也不需要连收银 PC**——只要连店里 wifi、能开网页就行。现有收银 PC 距离太远不是问题。

| 谁 | 用什么 | 说明 |
|---|---|---|
| **柜台（推荐）** | 便宜平板 + 蓝牙 2D 读码器（约 ¥20,000–30,000 + ¥8,000） | 平板壁挂 / 立架，开机进 kiosk 页面、整班次挂着。读码器选 **HID（键盘）模式**、**带充电底座**、**能读手机屏幕**。客人出示卡片二维码，读码器读入 → 店员在平板上输金额 → 确认。同一台平板也用来扫券核销 |
| 客人 | 自己的手机 | 不装 App，只用浏览器 |
| 店铺 | 店内固定二维码贴纸 | 桌上 + 柜台，印刷成本。仅登记入口 |
| **替代方案** | 店员自己的手机 | 0 硬件。员工页面加到主屏当 PWA，摄像头扫客人卡片码 → 输金额 → 确认。缺点：依赖个人手机、要摄像头权限、每次点几下 |

> **"什么都不用管" 和 "输金额" 有冲突**
> 输金额本身就是"管一下"。最轻的做法是平板上放 3 个金额档位按钮（〜¥2000 / 〜¥5000 / ¥5000+），店员点一下 2 秒，既快又保留消费额分级。想彻底零操作，只能抽奖不看金额（改成"每次到店 N 次"），并接受打卡可刷、¥500 券靠兑换时店长把关。

> **真正的零操作路径**
> 如果收款端将来能给 API 或付款 webhook（Square / Airレジ / スマレジ 这类平板 POS 都有），金额自动带过来，店员一下都不用点。评审稿写明当前 POS 无接口，所以这条路现在关着。

---

## 10. 权限与审计

沿用现有三角色 + `BranchScopedQuerysetMixin` / `IsAdminRole` / `DenyStaffRole`。

| 角色 | 能做 | 不能做 |
|---|---|---|
| `staff` | 创建消费确认；查看自己刚创建的确认结果；核销 / 兑换中奖券 | 管理活动和奖品；改概率 / 库存；指定中奖结果；删除或覆盖记录；查看所有客人完整信息 |
| `branch` | 管理本分店的活动、奖品、记录；查看本分店风险事件 | 看别的分店 / 别的 Organization 的数据；奖品库存是否可管待定 |
| `admin` | 本 Organization 内全部内容 | 跨 Organization（那是 Django superuser 的事） |

### 实现方式

- `staff` 的窄权限照 scheduling 里的自助模式：view 上 `permission_classes = [IsAuthenticated]` + 在 `get_queryset` / `perform_*` 里手动判 `role == STAFF`
- 消费确认**追加记录、不可删**——现成范本是 `inventory.StockTransaction`（不可变账本）、`dailyreports.DailyReportHistory`（append-only，who / when 从 request 取不信客户端）
- 修改消费金额 / 时间需 `admin` 权限 + 填原因；作废走独立的 `void` action，留 `voided_by` / `void_reason`
- 每条消费确认自动记 `verified_by`、分店、时间、来源 IP（用于风险识别）
- 大额消费 / 高价值奖品可要求二次确认

### 个人信息（APPI 底线，不是设计选择）

- 手机 + 姓名 + 生日 + 到店消费历史落在同一条客人记录上 = 个人情報
- 登记页需一句**告知**（收什么、做什么用）+ 客人勾选
- 后台需要一个**"按手机号删除该客人记录"**的操作，用于响应删除请求
- 明确一个保留期限（如最后到店满 2 年清理）

> **阶段 0 要定**：是不是每个 `staff` 账号都能做消费确认，还是加一个按账号 / 按员工的开关？

---

## 11. 数据模型

新增独立 app `promotions`（在 `store-admin-backend` 内，接入方式和最近的 `inventory` 一致：`INSTALLED_APPS` + `config/urls.py` 一行 include）。字段基于评审稿第 8 章，把 `GuestSession` 换成手机号为主键的 `Customer`，补充积分、里程碑、奖池分档与券。

### Campaign — 活动

`organization` · `branch` · name · description · **status** (draft / active / paused / ended) · starts_at · ends_at · **points_per_1000yen** (默认 10) · **points_per_draw** (默认 100) · **points_per_voucher** · voucher_yen_per_unit (¥100) · points_expire_months (12) · direct_draw_threshold_yen (可空 = 关闭双轨) · max_draws_per_verification · max_draws_per_customer_per_day · stamp_target (可空 = 关闭集章) · created_by · updated_by · created_at · updated_at

### Milestone — 里程碑（累计积分门槛，或作 Campaign 上的 JSON）

`campaign` · **points_threshold** (如 300 / 800 / 2500) · **reward_type** (drink / dessert / voucher / …) · reward_config (JSON：面额、指定菜) · voucher_expires_after_days · display_label

### Prize — 抽奖奖品（一行一档，含料理分档）

`campaign` · name · description · display_order · **weight** · total_stock · **remaining_stock** · **daily_stock** (每店每日) · active · **reward_type** (cash_voucher / drink / dessert / side_dish / chef_special / points_refund) · reward_config (JSON：面额 / menu_value_cap_yen / refund_points) · voucher_expires_after_days · **voucher_min_spend_yen** · requires_manual_approval · created_at · updated_at

### Customer — 客人记录（店铺客人卡）

`organization` · **phone** (未验证、按 org 唯一) · name (可空) · birthday_md (可空，只存月/日) · **card_token** (卡片二维码里的稳定 token) · **points_balance** (PointsLedger 的汇总) · stamp_count · first_seen_at · last_seen_at · last_activity_at · **status** (active / blocked) · risk_level · privacy_consented_at

### PointsLedger — 积分流水（不可变，像 StockTransaction）

`customer` · **delta** (正 = 赚，负 = 花) · **reason** (spend / draw / voucher / milestone / expire / adjust) · source_ref (FK：SpendVerification / LotteryDraw / Voucher) · balance_after · note · operator (调整时的 admin) · created_at

### CheckInRecord — 打卡记录

`customer` · branch · campaign · **spend_verification** (FK，打卡由它产生) · checked_in_at · **local_date** · result · risk_level
唯一约束：(customer, campaign, local_date)

### SpendVerification — 店员消费确认（全案最重要）

`customer` · check_in_record · **campaign** · **branch** · table_number · **amount_yen** (非负整数) · **consumed_at** (不能是未来) · **points_granted** (服务端计算) · direct_draws_granted (双轨开启时) · **verified_by** · verified_at · source_ip · status · risk_level · voided_at · voided_by · void_reason · created_at

### LotteryDraw — 抽奖记录

`campaign` · branch · **customer** · **source** (points / direct) · points_spent · spend_verification (direct 时) · **request_id** (唯一) · prize (可空) · **prize_name_snapshot** · **weight_snapshot** · drawn_at · status

### Voucher — 券（里程碑 / 抽奖 / 积分兑换统一）

`customer` · campaign · **source** (milestone / lottery / points_redeem) · source_ref (FK) · reward_type · config_snapshot (面额 / menu_value_cap) · **min_spend_yen** · **redemption_code** (唯一、单次) · branch · issued_at · **expires_at** · **status** (active / redeemed / expired / void) · redeemed_at · redeemed_by · approved_by

### RiskEvent — 风险记录

`organization` · branch · customer · staff_user · **event_type** · severity · **evidence** (JSON：依据、来源) · **status** · reviewed_by · reviewed_at · review_note · created_at

> **卡片 token 与手机号**
> `card_token` 是给读码器扫的（长、不可猜），`phone` 是人可报的主键。两者都指向同一条 `Customer`。丢了浏览器那份 token，用手机号照样查得回。去掉了评审稿里的匿名 key、6 位一次性码、设备指纹。

> **高频表要分页**
> 客人 / 打卡 / 消费确认 / 抽奖 / 兑换的增长速度远超日报。这几个后台列表**必须分页**，偏离项目现有"前端拉全量"的惯例。有先例：`purchasing` 的 `PurchasePagination`。

---

## 12. API 草案

### 公开客人 API — AllowAny + 限流

```
POST /api/guest/register/        { store_token, phone, name?, birthday_md?, consent } → 建 Customer + 返回 card_token
POST /api/guest/login/           { phone, birthday_md } → 只读会话（看卡 / 余额 / 历史，换设备用）
GET  /api/guest/card/            凭 card_token：积分余额、集章、券列表、积分流水、活动信息
POST /api/guest/redeem/          { type: "draw" | "voucher", request_id } → 扣积分 + 写流水 + 抽奖 / 出券
```

> **公开接口不接受的字段**：`table_number` · `amount_yen` · `consumed_at` · `points_granted` · `prize_id` · 积分余额直改 —— 这些只能从店员确认接口 / 服务端产生。"只读会话"不能核销券、不能改余额（花积分换券也只出下次券，仍需当面核销）。

### 店员 API — IsAuthenticated，视图内判 staff

```
POST /api/promotions/customers/lookup/          { card_token | phone } → 客人名 + 积分 + 集章 + 名下券
POST /api/promotions/spend-verifications/        { card_token | phone, amount_yen, table_number?, consumed_at? } → { check_in, points_granted, balance, risk_level }
GET  /api/promotions/spend-verifications/mine/   自己刚创建的
POST /api/promotions/redemptions/verify/         { redemption_code } → 券内容 + 有效期
POST /api/promotions/redemptions/confirm/        { redemption_code } → 核销
```

### 管理员 / 分店 API — 沿用 Organization / Branch 隔离

```
CRUD   /api/promotions/campaigns/                             含 /{id}/prizes/
GET    /api/promotions/customers/                             按手机 / 姓名搜索，分页；含积分流水
POST   /api/promotions/customers/{id}/points-adjust/          { delta, note } · admin only · 写 PointsLedger
DELETE /api/promotions/customers/{id}/                        响应客人删除请求（APPI）· admin only
GET    /api/promotions/campaigns/{id}/checkins|verifications|draws|redemptions/   分页
GET    /api/promotions/risk-events/
POST   /api/promotions/risk-events/{id}/review/
POST   /api/promotions/spend-verifications/{id}/void/         { reason } · admin only
POST   /api/promotions/redemptions/{code}/approve/            高价值奖品店长确认
```

---

## 13. 反作弊与风险规则

### 结构性防护（靠设计，不靠规则）

- 客人自助只能"登记 + 打开卡片页 + 花自己已有的积分"——积分只在"店员在收银台点确认"那一下产生
- 只读会话（手机 + 生日）不能核销券、不能改余额
- 抽奖 / 花积分在服务端事务内完成，余额行锁 + 权重快照 + 库存行锁 + 幂等 request_id
- 积分是不可变流水的汇总，改动留 operator + note
- 券单次核销、绑分店、带有效期、有使用门槛、绑客人记录、顶奖店长确认

### 规则型风险标记（标记，不自动定作弊）

| 处理 | 风险情况 |
|---|---|
| **拒绝** | 消费时间晚于当前时间 · 非营业时间确认（或要求店长确认）· 同一客人同一营业日重复确认 |
| **暂停 / 提示** | 同一客人短时间多次抽奖 · 同一客人每日抽奖到上限 |
| **标记 + 通知** | 同一店员短时间确认大量消费 · 大量金额刚好等于门槛 · 同一设备短时间登记多个手机号 · 同一手机号在多店高频出现 |
| **标记 + 人工兑换** | 高价值奖品连续集中中奖 |
| **管理员介入** | 退款 / 作废后的消费确认 → 撤销剩余次数、冻结兑换 |

风险标记不等于作弊结论。后台需显示依据、来源和处理状态，避免误伤正常客人。

### 运营报表（阶段 3）

- 每店 / 每员工的消费确认异常报表
- 金额和时间抽查
- 与 POS 当日总额人工核对
- 店员操作统计

### 限流基建

项目目前**没有配置共享缓存**（默认 `LocMemCache`，每个 gunicorn worker 独立，做限流不可靠）。DRF 自带 throttle / `django-ratelimit` 都依赖共享缓存。

> **阶段 0 要定**：共享缓存选型——DB 缓存表（`createcachetable`，零新增容器，推荐 v1）还是 Redis（改 docker-compose，加一个容器）。

---

## 14. 已接受的现实限制

> **店员是人工信任点**
> POS 不提供小票编号 / 交易号 / API 时，系统无法独立证明店员输入的消费额真实。以下无法从技术上完全阻止，只能靠审计降低：店员与客人串通输入虚假消费额 · 把同一笔消费分配给多个客人 · 把高额消费重复确认。**建议评审时让店主方书面接受。** 将来 POS 能给交易号时，可升级为自动核验。

> **客人用别人的号 / 假号 —— 明确可接受**
> 不做手机验证的直接后果。判断依据：客人人到了店、消费了才有价值产生，送的东西损失有限。代价是客户库里混入假号和重复登记，做生日营销、统计会员数时打折。不影响抽奖 / 集章闭环。

> **丢卡 / 报错号 = 积分和券找不回**
> 清浏览器 / 换手机后 card_token 没了，店员按手机号能查回——**前提是客人报对同一个号**。报错号或懒得报，就当新客重新登记一张，积分余额、集章、未兑换的券都跟着丢。只读登录（手机 + 生日）是给这种情况的找回口。

> **账户被窥 / 券冒用**
> 手机 + 生日能"登录看"余额和历史（生日常在社媒上，不是强锁）。但**只能看不能花**——花积分只出下次券，用券要当面找店员核销。远程知道你手机 + 生日的人看得到、拿不走。

> **促销总成本是另一道账**
> 每笔抽奖 / 券背后都有真实消费，但不保证促销不亏——取决于 **1% 返点 + 抽奖 EV + 里程碑 + 集章的合计 < 客流增量带来的边际利润**。阶段 0 用预估到店数算月度总成本，上线后用真实核销率校准（见 §6 调节杆）。

---

## 15. 需要拍板的决定（阶段 0）

| # | 问题 | 建议 |
|---|---|---|
| 01 | 积分比例：赚分率 / 换抽奖 / 换券？ | ¥1,000→10 分；100 分→1 抽 或 ¥100 券。上线后用真实核销率校准 |
| 02 | 里程碑档位（累计积分）？ | 300 / 800 / 2500 分 → 饮料 / 甜品 / ¥500 券 |
| 03 | 要不要保留独立"按到店次数集章"，还是只按积分？ | 保留一个简单集章（满 5 次送饮料 / bonus 积分），奖励高频客 |
| 04 | 双轨：当次消费达门槛直接送 1 抽？ | v1.1 先不做，只走积分换抽；看客人反馈再加 |
| 05 | 确认 §6 奖池：权重、每日库存、券有效期、料理成本上限？ | 按 §6 表；主厨指定菜单价上限 ¥1,200；¥5,000 券每日 1 张 / 店 |
| 06 | 抽奖 / 花积分上限：每位客人每日？ | 每人每日 10 抽；积分过期 12 个月 |
| 07 | 营业日切换时间（跨午夜营业的店）？ | 每分店可配，默认 05:00 |
| 08 | 每个 `staff` 账号都能做消费确认，还是加开关？ | v1 全员可做，加账号级开关留到阶段 3 |
| 09 | `branch` 账号能否管理奖品和库存，还是仅 `admin`？ | branch 可管本店活动，奖品库存仅 admin |
| 10 | 共享缓存：DB 缓存表还是 Redis？ | v1 用 DB 缓存表，零新增容器 |
| 11 | 生产域名 + HTTPS 现状？（二维码是公网链接的前提） | 上线前必须有域名 + HTTPS；没有则先处理 |
| 12 | 首次登记的必填 / 选填项？ | 手机号必填；姓名、生日月日选填（生日用于生日券）。不做手机验证 |
| 13 | 客人记录保留期限？ | 最后到店满 2 年自动清理（APPI 保留期限） |
| 14 | 店员平板输金额：手输还是档位按钮？ | v1 手输（准）；忙不过来再加档位按钮 |
| 15 | 客人页面语言？ | 日文优先，可选中日切换；管理端维持中日双语 |

---

## 16. 实施阶段

### 阶段 0：规则确认

- 确认 §15 的全部 15 项
- 确认奖品兑换流程、哪些需店长确认

### 阶段 1：客人卡 + 打卡 + 消费确认 + 赚积分

- 新 app `promotions`；`Customer` 记录（手机号 + card_token，浏览器 cookie + localStorage 滚动续期）；`PointsLedger`
- 店内固定二维码 token 校验；客人登记页（手机号 + 选填姓名/生日 + 隐私告知）；只读登录（手机 + 生日）；客人卡片页（积分余额 / 流水 / 集章 / 券）
- 店员平板 kiosk 页（读码器读 card_token / 手输手机号 → 金额 → 确认）
- 打卡记录、消费确认 → 服务端发积分（+ 集章 +1）；积分流水
- 基础权限（staff 窄权限）+ 审计字段 + 作废 action + 按手机号删除客人记录 + points-adjust

### 阶段 2：抽奖 + 奖池 + 券

- 后台活动管理 + 奖品分档 / 权重 / 库存 CRUD（照 inventory / products 套路）
- 服务端安全随机加权抽奖；中奖记录（权重快照）；`Voucher` 生成
- 券核销页（按 card_token / 手机号查名下券，使用门槛校验，顶奖店长确认）
- 并发与重复提交测试（`select_for_update` + 幂等 request_id）

### 阶段 2.5：花积分（抽奖 / 换券 / 里程碑）— 用真实数据校准

- `/api/guest/redeem/`：积分 → 抽奖次数 / ¥100 抵用券（事务内扣余额 + 写流水）
- 里程碑判定（累计积分达门槛 → 发券）
- 积分过期定时任务
- 拿阶段 1–2 的真实消费 / 核销数据，校准奖池权重让"一抽 EV ≈ ¥100"
- （可选）双轨：当次消费直接送 1 抽

### 阶段 3：反作弊与运营报表

- 共享缓存 + IP / 设备 / 接口限流
- 规则型风险标记 + 风险记录后台
- 消费确认抽查报表、店员操作统计、积分 / 奖品出货月报
- staff 账号级"可确认"开关

### 阶段 4：可选增强（不含 LINE）

- 平板上金额档位按钮（替代手输金额）
- 退款、作废记录的人工联动（撤销积分 / 冻结券）
- 生日券自动发放；Apple / Google Wallet 卡片
- 将来接入能开 API 的收款端后，金额自动带入 / 自动核验

---

## 17. 工作量评估

加了积分层后，体量比 `scheduling` + `wages` 之和还大一些。单人开发**数周到一两个月**，跨前后端几千行。**没有任何单点是技术上冒险的**——难度在"量"和"细节完整性"。**建议按阶段上线**：阶段 1–2 先跑起来（客人卡 + 打卡 + 赚分 + 抽奖），拿到真实数据再上阶段 2.5 的花积分并校准。

| 阶段 | 后端 | 前端 |
|---|---|---|
| 阶段 1 | 新 app、~9 模型、4 公开接口 + 6 店员接口、客人记录 + card_token + 积分流水、审计权限、删除 / 调分接口 | 客人登记页 + 只读登录 + 卡片页（余额 / 流水 / 集章）、店员平板 kiosk 页；新增 public 路由 + 轻布局 |
| 阶段 2 | 抽奖 service、加权算法、奖品分档、券生成 + 核销、库存行锁、并发测试 | 后台活动 / 奖品 CRUD、券核销页、客人抽奖页 + 动画 |
| 阶段 2.5 | 积分兑换 service（抽奖 / 换券）、里程碑判定、过期任务、奖池校准 | 卡片页"换抽奖 / 换券"操作 |
| 阶段 3 | 缓存 + 限流、风险规则引擎、报表聚合 | 风险记录后台、报表页 |

前端 guest 专区建议**复用现有 SPA + public 路由**（`meta.public` 机制已存在），不做独立打包。二维码渲染用前端 `qrcode` npm 包（后端只发 token）。店员平板页可以是同一个 SPA 的一个 staff 路由，读码器是 HID 键盘模式、不需要额外前端集成（`inventory` 的 `lookup` 已是同款）。

---

## 18. 第一版默认配置

| 项目 | 默认值 |
|---|---|
| 客人识别 | 手机号 + 卡片二维码 |
| 只读登录 | 手机号 + 生日月日 |
| 首次登记 | 手机必填 · 姓名/生日选填 |
| 手机验证 / LINE | 都不做 |
| 店内二维码 | 固定 · 仅登记入口 |
| 赚积分 | ¥1,000 → 10 分（1%） |
| 积分换抽奖 | 100 分 / 次 |
| 积分换券 | 100 分 → ¥100 |
| 里程碑 | 300 / 800 / 2500 分 |
| 积分过期 | 末次活动 + 12 个月 |
| 抽奖时机 | 走人后 · 自己手机 |
| 奖品形态 | 全部下次到店券 |
| 顶奖 | ¥5,000 券封顶 · 每日 1 张/店 · 店长确认 |
| 券使用门槛 | 高价值券需满额 |
| 定位 / Turnstile | 关闭 |
| 消费确认修改 | 仅 admin · 必填原因 |
| 客人记录保留 | 末次到店 + 2 年 |
| 共享缓存 | DB 缓存表 |
| 新增设备 | 柜台平板 + 蓝牙读码器 |

---

*本稿整理自 2026 年 8 月关于评审稿的讨论。前提：客人皆敌、店员可信、不做手机验证、不接 LINE、不做会员等级。硬件仅柜台一台平板 + 蓝牙读码器。数据模型与 API 为草案，实现时以代码库现有约定为准。*
