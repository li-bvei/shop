# 餐饮店客人打卡与消费抽奖功能方案

## 1. 文档状态

- 状态：方案评审稿
- 本文档只描述产品、数据和技术方案，不包含具体实现
- 当前前端：Vue 3 + TypeScript + Element Plus
- 当前后端：Django 4.2 + Django REST Framework + MySQL
- 当前权限角色：`admin`、`branch`、`staff`
- 现有系统已经具备 Organization / Branch 多租户隔离和 JWT 登录

本文档根据以下前提设计：

> POS 不能提供小票编号或 API，但店员可以查看客人的桌号、消费额和消费时间。桌号、消费额、消费时间只能由店员提供，客人不能自行填写。

---

## 2. 产品目标

### 2.1 客人端目标

- 客人到店后扫描二维码即可打卡
- 不要求注册账号、设置密码、下载 App
- 客人可以在满足消费条件后参加抽奖
- 客人只需要扫描二维码、确认页面，不需要填写桌号、金额和时间
- 中奖后可以直接向店员展示兑换页面

### 2.2 店铺后台目标

- 管理员可以创建抽奖活动
- 可以自定义奖品种类、中奖概率、库存和消费门槛
- 可以查看打卡、消费确认、抽奖、领奖和风险记录
- 店员可以确认客人的桌号、消费额和消费时间
- 店员不能修改奖品概率、库存和活动规则
- 所有关键操作都可以追溯到具体店员、时间和分店

### 2.3 不做的事情

- 第一版不强制要求客人注册
- 第一版不要求 POS 系统提供接口
- 第一版不让客人自行输入消费额
- 第一版不把浏览器定位作为唯一的打卡依据
- 第一版不承诺完全消除店员与客人串通作弊的可能性

---

## 3. 核心设计决定

### 3.1 客人不注册

客人首次扫码时，系统自动创建一个匿名访客会话：

- 使用随机生成的访客编号
- 使用短期签名会话令牌
- 令牌保存在浏览器 Cookie 或会话存储中
- 不要求姓名、电话、邮箱或社交账号
- 客人只有在需要保存中奖记录、通知或高价值领奖时，才可以自愿填写联系方式

匿名会话不能等同于绝对身份。客人清理浏览器数据、更换手机或使用多个设备时，系统无法百分之百识别为同一个人。因此需要和动态二维码、消费确认、频率限制和审计一起使用。

### 3.2 店员是消费信息的唯一来源

桌号、消费额和消费时间不从客人端取得。

客人端只能提供：

- 到店二维码
- 匿名访客会话
- 一次性确认码

店员端必须提供：

- 一次性确认码
- 桌号
- 消费金额（日元）
- 消费时间

服务器只接受店员登录后的消费确认请求。客人端即使修改网页、调用接口或伪造请求，也不能直接创建消费资格。

### 3.3 消费金额换算抽奖次数

默认规则采用整除：

```text
抽奖次数 = floor(消费金额 / 消费门槛)
```

例如消费门槛为 1,000 日元：

| 消费金额 | 抽奖次数 |
|---:|---:|
| 999 日元 | 0 |
| 1,000 日元 | 1 |
| 2,500 日元 | 2 |
| 5,000 日元 | 5 |

活动可以设置每桌、每次消费、每位匿名访客和每天的最高次数，防止高额消费或重复确认造成异常抽奖。

### 3.4 抽奖必须由服务器完成

- 前端只显示动画和结果
- 奖品选择在后端完成
- 使用安全随机数
- 使用数据库事务锁定抽奖次数和奖品库存
- 防止用户连续点击导致重复抽奖
- 抽奖记录保存当时的奖品概率快照
- 后续调整概率不会改变历史结果

---

## 4. 客人端流程

### 4.1 到店打卡流程

```text
店内展示动态二维码
        ↓
客人扫码
        ↓
打开公开打卡页面
        ↓
服务器验证二维码、分店、活动和有效期
        ↓
自动建立匿名访客会话
        ↓
完成当天打卡
        ↓
显示“请向店员确认消费信息”
```

客人不需要填写桌号、消费金额或消费时间。

### 4.2 获取抽奖资格流程

```text
客人完成打卡
        ↓
点击“申请抽奖资格”
        ↓
页面显示 6 位一次性确认码和二维码
        ↓
客人向店员展示确认码
        ↓
店员查看 POS 上的桌号、消费金额和消费时间
        ↓
店员在员工页面提交消费确认
        ↓
服务器按活动规则计算抽奖次数
        ↓
客人页面自动显示可抽奖次数
```

一次性确认码建议：

- 6 位数字或短字母数字组合
- 有效期 3 分钟
- 只能绑定一个匿名访客会话
- 成功消费确认后立即失效
- 超时后可以重新生成，但旧码不能继续使用

### 4.3 抽奖流程

```text
客人点击“开始抽奖”
        ↓
服务器检查剩余次数、活动状态和风险状态
        ↓
事务内扣减次数并锁定奖品库存
        ↓
服务器按照权重抽取奖品
        ↓
保存抽奖记录
        ↓
返回中奖结果和兑换编号
```

页面需要明确显示：

- 当前剩余次数
- 活动有效时间
- 消费门槛
- 中奖结果
- 兑换方式和有效期
- “谢谢参与”也要生成记录，避免重复请求

### 4.4 领奖流程

- 客人向店员展示中奖结果页
- 店员输入或扫描兑换编号
- 系统检查是否属于当前分店、是否已兑换、是否过期
- 兑换成功后记录兑换店员和兑换时间
- 高价值奖品可以增加管理员确认

如果需要长期保存中奖记录或发送通知，再提示客人自愿填写电话、邮箱或 LINE 信息。普通抽奖不要求填写。

---

## 5. 店员端流程

### 5.1 店员消费确认页面

建议新增一个简化的员工页面，例如：

```text
/staff/promotion-verification
```

页面只包含：

1. 扫描或输入客人确认码
2. 桌号
3. 消费金额
4. 消费时间
5. 确认按钮

店员提交后显示：

- 本次确认是否成功
- 获得几次抽奖机会
- 是否触发风险提醒
- 确认编号

店员不能在此页面：

- 修改抽奖概率
- 修改奖品库存
- 直接选择中奖奖品
- 删除历史确认记录
- 修改其他店员已经提交的金额

### 5.2 消费时间规则

消费时间必须由店员根据 POS 信息填写。

服务器应限制：

- 不能填写未来时间
- 不能超过活动允许的历史时间窗口
- 默认只能填写当前营业日的消费时间
- 跨午夜营业的店铺可以配置营业日切换时间
- 修改消费时间需要管理员权限并留下原因

### 5.3 桌号规则

桌号主要用于审计和防止同一桌重复确认，不作为客人的身份认证。

建议：

- 每次确认都必须填写桌号
- 桌号可以是店铺自定义编号，例如 `A01`、`テーブル3`
- 同一匿名访客、同一桌号、相近时间内重复确认时提示风险
- 不能只凭桌号自动获得抽奖次数

### 5.4 店员权限

现有 `staff` 角色可以增加一个非常窄的权限范围：

- 允许创建消费确认
- 允许查看自己刚刚创建的确认结果
- 允许验证和兑换中奖记录
- 不允许管理活动和奖品
- 不允许查看所有客人的完整信息
- 不允许删除或覆盖记录

`admin` 可以管理全部内容。

`branch` 可以管理自己分店的活动、奖品和记录，具体是否允许管理奖品库存可以再确认。

---

## 6. 打卡二维码设计

### 6.1 动态二维码

不建议使用永久不变的二维码。建议二维码每 60 秒轮换一次。

二维码内容只包含一个短链接和临时令牌，例如：

```text
https://example.com/guest/check-in?t=temporary-token
```

令牌由服务器生成，包含或关联：

- 分店
- 活动
- 用途：打卡或抽奖入口
- 生成时间
- 过期时间
- 随机 nonce
- 令牌状态

数据库只保存令牌的 hash，不保存可直接使用的明文令牌。

### 6.2 二维码展示方式

后台新增二维码展示页：

- 当前分店名称
- 当前活动名称
- 当前二维码
- 自动倒计时刷新
- 手动刷新按钮
- 适合电视、平板和打印的显示模式
- 二维码失效时显示刷新提示

打印版二维码可以作为备用，但打印二维码只能用于打开入口，不能绕过消费确认和次数限制。

### 6.3 是否每天更换

“每天更换”可以作为额外措施，但不够充分。推荐组合：

- 二维码令牌每 60 秒更换
- 每天生成新的活动密钥或二维码批次
- 旧批次自动失效
- 服务端始终检查有效期、分店和活动状态

这样即使有人拍照转发，二维码也只能在很短时间内有效。

---

## 7. 反作弊方案

### 7.1 客人端防刷

- 动态二维码和短期令牌
- 一次性确认码
- 一次性消费确认
- 一张确认码不能重复使用
- 同一访客每天每店打卡次数限制
- 同一访客每天抽奖次数上限
- 同一消费确认不能重复发放次数
- 抽奖接口限流
- 防止重复点击的幂等请求编号
- 异常请求触发无感 Turnstile 或人工确认

### 7.2 店员端防作弊

- 所有消费确认必须绑定登录店员
- 自动记录店员账号、分店、时间、IP 和设备会话
- 消费确认采用追加记录，不能直接覆盖
- 修改需要管理员和修改原因
- 大额消费或高价值奖品可以要求二次确认
- 店员不能指定中奖结果
- 店员不能直接增加抽奖次数
- 店员不能删除异常记录

### 7.3 异常规则

系统可以先做规则型风险标记，不直接自动判定作弊：

| 风险情况 | 建议处理 |
|---|---|
| 同一确认码重复提交 | 直接拒绝并记录 |
| 同一访客短时间多次申请 | 暂停并提示稍后再试 |
| 同一设备连接多个访客会话 | 标记风险 |
| 同一店员短时间确认大量消费 | 标记风险并通知管理员 |
| 大量金额刚好等于抽奖门槛 | 标记风险 |
| 非营业时间确认消费 | 拒绝或要求管理员确认 |
| 消费时间晚于当前时间 | 直接拒绝 |
| 同桌短时间反复创建确认 | 标记风险 |
| 高价值奖品连续集中中奖 | 标记风险并要求人工兑换 |
| 退款或作废后的消费确认 | 管理员撤销剩余资格或冻结兑换 |

风险标记不等于作弊结论。后台需要显示依据、来源和处理状态，避免误伤正常客人。

### 7.4 定位和验证码

定位可以作为辅助信号：

- 普通客人默认不强制开启定位
- 风险较高时请求浏览器定位
- 例如判断是否在店铺 50–100 米范围内
- 定位失败时不一定立即拒绝，可以转入店员确认或 Turnstile

定位可能被拒绝或伪造，因此不能单独作为安全依据。

Cloudflare Turnstile 可以配置为无感或托管模式，只对异常请求触发，减少普通客人的操作。它需要正式域名、HTTPS 和 Cloudflare 配置，属于可选外部服务。

### 7.5 无法完全防止的风险

在 POS 没有提供可验证的小票编号、交易编号或 API 的前提下，系统无法独立证明店员输入的消费额真实。

因此以下情况无法从技术上完全阻止：

- 店员和客人串通输入虚假消费额
- 店员把同一笔消费分配给多个客人
- 店员把高额消费重复确认

应通过以下方式降低风险：

- 店员账号审计
- 消费确认不可删除
- 每店、每员工的异常报表
- 金额和时间抽查
- 高价值奖品二次确认
- 与 POS 当日总额进行人工核对

如果将来 POS 可以提供交易编号或订单时间，本方案可以升级为自动核验，安全性会明显提高。

---

## 8. 数据模型建议

建议在后端新增独立 Django app，例如 `campaigns` 或 `promotions`，不要把逻辑直接塞入 `dailyreports`。

### 8.1 Campaign：抽奖活动

字段建议：

- `id`
- `organization`
- `branch`
- `name`
- `description`
- `status`：draft / active / paused / ended
- `starts_at`
- `ends_at`
- `spend_threshold_yen`
- `draws_per_threshold`
- `max_draws_per_verification`
- `max_draws_per_guest_per_day`
- `requires_check_in`
- `qr_rotation_seconds`
- `check_in_radius_meters`（可选）
- `created_by`
- `updated_by`
- `created_at`
- `updated_at`

### 8.2 Prize：奖品

- `id`
- `campaign`
- `name`
- `description`
- `display_order`
- `weight`
- `probability_snapshot_label`（显示用，可选）
- `total_stock`
- `remaining_stock`
- `daily_stock`
- `active`
- `requires_manual_approval`
- `expires_after_days`
- `created_at`
- `updated_at`

概率建议使用权重保存，而不是要求所有奖品概率总和必须正好等于 100%。例如：

```text
一等奖：1
二等奖：5
三等奖：20
谢谢参与：74
```

实际概率由服务器根据所有启用奖品的权重计算。

### 8.3 GuestSession：匿名客人会话

- `id`
- `organization`
- `branch`
- `campaign`
- `anonymous_key_hash`
- `first_seen_at`
- `last_seen_at`
- `last_ip_hash`
- `device_session_hash`
- `status`
- `risk_level`
- `expires_at`

不保存不必要的姓名、电话和完整浏览器指纹。

### 8.4 CheckInQrSession：动态二维码会话

- `id`
- `branch`
- `campaign`
- `purpose`
- `token_hash`
- `issued_at`
- `expires_at`
- `used_at`
- `issued_by`
- `status`

### 8.5 CheckInRecord：打卡记录

- `id`
- `guest_session`
- `branch`
- `campaign`
- `qr_session`
- `checked_in_at`
- `local_date`
- `latitude` / `longitude`（只有启用定位时保存，并应降低精度）
- `ip_hash`
- `device_session_hash`
- `result`
- `risk_level`

建议添加唯一约束，防止同一访客在同一活动、同一营业日重复打卡。

### 8.6 SpendVerification：店员消费确认

这是本方案最重要的模型。

- `id`
- `guest_session`
- `check_in_record`
- `campaign`
- `branch`
- `verification_code_hash`
- `table_number`
- `amount_yen`
- `consumed_at`
- `draws_granted`
- `draws_remaining`
- `verified_by`
- `verified_at`
- `status`
- `risk_level`
- `voided_at`
- `voided_by`
- `void_reason`
- `created_at`

重要约束：

- `table_number`、`amount_yen`、`consumed_at` 只能由店员请求写入
- 消费金额必须为非负整数
- 消费时间不能是未来时间
- 消费确认不能由客人端创建
- 一次确认成功后不能被普通店员修改
- `draws_granted` 必须由服务器计算

### 8.7 LotteryDraw：抽奖记录

- `id`
- `campaign`
- `branch`
- `guest_session`
- `spend_verification`
- `request_id`
- `prize`
- `prize_name_snapshot`
- `weight_snapshot`
- `drawn_at`
- `status`
- `redemption_code`
- `expires_at`
- `redeemed_at`
- `redeemed_by`

`request_id` 必须唯一，防止重复提交造成重复中奖。

### 8.8 RiskEvent：风险记录

- `id`
- `organization`
- `branch`
- `guest_session`
- `staff_user`
- `event_type`
- `severity`
- `evidence`
- `status`
- `reviewed_by`
- `reviewed_at`
- `review_note`
- `created_at`

---

## 9. API 草案

### 9.1 公开客人 API

公开接口不要求登录，但必须经过动态令牌、会话和限流检查。

```text
POST /api/guest/sessions/
POST /api/guest/check-ins/validate-qr/
POST /api/guest/check-ins/
POST /api/guest/verification-codes/
GET  /api/guest/lottery/status/
POST /api/guest/lottery/draw/
GET  /api/guest/draws/{id}/
POST /api/guest/redemptions/{code}/status/
```

公开接口不能接受以下字段作为资格来源：

- `table_number`
- `amount_yen`
- `consumed_at`
- `draws_granted`
- `prize_id`

这些字段只能从店员端确认接口产生。

### 9.2 店员 API

```text
POST /api/promotion-verifications/
GET  /api/promotion-verifications/mine/
POST /api/redemptions/verify/
```

创建消费确认请求至少需要：

```json
{
  "verification_code": "A7K29P",
  "table_number": "A03",
  "amount_yen": 2500,
  "consumed_at": "2026-08-30T19:20:00+09:00"
}
```

服务器响应示例：

```json
{
  "verification_id": 123,
  "draws_granted": 2,
  "draws_remaining": 2,
  "status": "accepted",
  "risk_level": "normal"
}
```

### 9.3 管理员 API

```text
GET/POST/PATCH/DELETE /api/campaigns/
GET/POST/PATCH/DELETE /api/campaigns/{id}/prizes/
GET                    /api/campaigns/{id}/draws/
GET                    /api/campaigns/{id}/check-ins/
GET                    /api/campaigns/{id}/verifications/
GET                    /api/risk-events/
POST                   /api/risk-events/{id}/review/
POST                   /api/verifications/{id}/void/
POST                   /api/redemptions/{code}/approve/
```

所有后台接口必须继续沿用当前 Organization 和 Branch 隔离规则。

---

## 10. 前端页面建议

### 10.1 管理后台

建议新增以下菜单：

- 营销活动
- 抽奖活动设置
- 奖品管理
- 二维码展示
- 打卡记录
- 消费确认记录
- 抽奖记录
- 兑换记录
- 风险记录

抽奖活动编辑页面需要提供：

- 消费门槛
- 抽奖次数上限
- 打卡限制
- 活动时间
- 二维码刷新间隔
- 奖品和权重
- 库存
- 高价值奖品人工确认开关

### 10.2 店员页面

店员页面必须足够简单：

```text
确认客人消费

客人确认码     [输入或扫描]
桌号           [输入]
消费金额       [输入]
消费时间       [默认当前时间，可调整]

[确认消费]
```

默认时间只是方便输入，最终仍以店员提交值为准，并由服务器校验。

### 10.3 客人页面

客人页面建议保持 3 个步骤：

1. 打卡成功
2. 向店员展示确认码
3. 抽奖和查看结果

页面不显示需要客人填写的桌号、金额和时间输入框。

---

## 11. 开源方案复用建议

### 11.1 可以复用的部分

- [django-qr-code](https://django-qr-code.readthedocs.io/latest/)：二维码生成和显示
- [django-ratelimit](https://github.com/jsocol/django-ratelimit)：接口和 IP 级限流
- Django REST Framework 自带 throttling：公开接口的基础限流
- [Cloudflare Turnstile](https://developers.cloudflare.com/turnstile/)：异常请求的无感人机验证

### 11.2 只参考、不直接作为基础的项目

- [Qiaomu Lucky](https://github.com/joeseesun/qiaomu-lucky)：有活动、奖品权重、记录和导出功能，但使用 Node.js 和本地 JSON 文件，不符合本项目 Django、MySQL 和多租户结构。
- [AURA](https://github.com/PauloKT/TCC-AURA)：有动态二维码、GPS 和 WebAuthn，但属于教学性质项目，不能直接当作生产级考勤基础。

### 11.3 不建议直接复制的内容

- 不直接复制完整的用户系统
- 不直接复制单机 JSON 数据结构
- 不把前端抽奖随机算法当作安全逻辑
- 不直接使用永久二维码
- 不使用“客人输入消费额”的方案

---

## 12. 实施阶段

### 阶段 0：规则确认

- 确认消费门槛和抽奖次数计算方式
- 确认是否必须先打卡才能抽奖
- 确认每天、每桌、每位客人的上限
- 确认营业日切换时间
- 确认奖品兑换流程
- 确认哪些奖品需要管理员确认

### 阶段 1：打卡和店员消费确认

- 新增匿名访客会话
- 新增动态二维码
- 新增客人打卡页面
- 新增一次性确认码
- 新增店员消费确认页面
- 新增桌号、消费额、消费时间记录
- 新增基础权限和审计

### 阶段 2：抽奖核心功能

- 新增活动管理
- 新增奖品和权重管理
- 新增库存
- 新增抽奖次数计算
- 新增服务端安全随机抽奖
- 新增中奖记录和兑换编号
- 增加并发和重复提交测试

### 阶段 3：反作弊和运营报表

- 增加 IP、设备和接口限流
- 增加风险规则
- 增加风险记录后台
- 增加高价值奖品人工确认
- 增加消费确认抽查报表
- 增加店员操作统计

### 阶段 4：可选增强

- 高风险时请求定位
- 高风险时启用 Turnstile
- 客人自愿绑定手机号或 LINE
- 退款、作废记录的人工联动
- 将来接入 POS 交易编号后自动核验

---

## 13. 验收标准

### 客人端

- 不注册也可以完成打卡
- 不填写桌号、金额和时间也可以操作
- 过期二维码不能打卡
- 同一访客当天重复打卡受到限制
- 没有店员确认时不能获得抽奖次数
- 客人不能通过修改请求参数伪造消费金额
- 连续点击不会重复扣次数或重复中奖

### 店员端

- 只有登录店员可以提交消费确认
- 桌号、消费额和消费时间必填
- 消费金额只能是合法的日元整数
- 不能填写未来时间
- 提交后普通店员不能删除或修改
- 记录中能看到提交店员和提交时间
- 店员不能选择奖品或修改中奖概率

### 管理后台

- 可以设置奖品种类、权重、库存和门槛
- 可以查看消费确认、打卡、抽奖和兑换记录
- 可以查看风险事件和处理状态
- 不同 Organization 之间不能互相看到数据
- 分店账号只能看到自己的分店
- staff 账号不能进入活动和奖品管理

### 抽奖公平性

- 抽奖结果只能由服务器产生
- 奖品库存不会被并发请求扣成负数
- 概率修改不会改变过去的中奖记录
- 每条抽奖记录都能追溯到访客、消费确认和店员

---

## 14. 推荐的第一版默认配置

| 项目 | 默认值 |
|---|---|
| 客人注册 | 不需要 |
| 二维码刷新 | 60 秒 |
| 确认码有效期 | 3 分钟 |
| 抽奖资格来源 | 店员确认 POS 消费额 |
| 消费门槛 | 1,000 日元，可配置 |
| 抽奖次数计算 | 消费额整除门槛 |
| 同一访客每日打卡 | 1 次/店 |
| 同一消费确认 | 只能使用 1 次 |
| 定位 | 默认关闭，风险时可开启 |
| Turnstile | 默认关闭，风险时开启 |
| 高价值奖品 | 店员兑换 + 管理员确认 |
| 客人联系方式 | 非必填，仅在领奖或通知时收集 |
| 消费确认修改 | 仅管理员，必须填写原因 |

---

## 15. 最终建议

第一版采用以下闭环：

> 客人扫码打卡 → 客人展示一次性确认码 → 店员查看 POS 并输入桌号、消费额、消费时间 → 服务器计算抽奖次数 → 客人抽奖 → 店员验证兑换。

这个方案符合当前 POS 能力，客人操作最少，也能保证消费额不会由客人自行伪造。

需要接受的现实限制是：没有 POS 交易编号或接口时，店员仍然是消费信息的人工信任点。因此第一版的重点应放在“店员权限最小化、记录不可删除、异常可追溯、金额可抽查”，而不是强行增加复杂的注册或身份验证流程。

