# 店铺管理系统项目整体说明

> 检查基准：2026-09-01
> 
> 本文基于当前工作区源码、配置、测试和 Markdown 文档整理。当前工作区存在未提交改动；本文描述的是当前工作区状态，不代表 `main` 分支已提交状态。

## 1. 项目定位

这是一个面向餐饮门店/店铺集团的前后端分离管理系统，主要覆盖：

- 经营看板与月度经营分析
- 每日报表、支付方式和报销记录
- 进货、供应商、品项价格比较与库存
- 员工、跨店调动、排班、实际出勤和简化工资计算
- 基于 Organization 的多租户隔离与 admin / branch / staff 角色权限
- 两套抽奖相关业务：历史抽奖名单管理，以及新建的积分卡/消费抽奖营销闭环

当前不是多个独立项目，而是一套 `shop` 项目，包含一个 Django 后端和一个 Vue SPA 前端。

## 2. 目录结构

```text
shop/
├── docker-compose.yml             MySQL 8、Django、Nginx/Vue 的部署编排
├── .env.example                   Compose 数据库启动变量示例
├── design_mockup.html             前端视觉设计参考
├── 开发进度.md                     历史开发记录与限制
├── 抽奖与打卡功能方案.md            早期匿名打卡方案评审稿
├── 打卡与抽奖实施方案.md            当前手机号积分卡方案
├── promotions-开发任务书.md         阶段任务与验收标准
├── store-admin-backend/            Django + DRF API
└── store-admin-frontend/           Vue 3 + TypeScript SPA
```

## 3. 技术架构

### 后端

- Django 4.2、Django REST Framework、MySQL 8
- PyMySQL 纯 Python 驱动
- Simple JWT 登录、刷新和角色信息
- django-filter 查询过滤
- DatabaseCache 作为共享数据库缓存，缓存表由 promotions 迁移创建
- Gunicorn 提供生产 WSGI 服务

后端以 Django app 按业务域拆分，API 统一挂在 `/api/` 下。金额由服务端计算，关键流水使用事务和 `select_for_update()` 保护。

### 前端

- Vue 3、`<script setup>`、TypeScript、Vite
- Element Plus、Pinia、vue-i18n
- ECharts 看板和月度分析图表
- `src/api/*.ts` 是唯一数据层，负责 HTTP 请求和 snake_case / camelCase DTO 转换
- 管理端使用 JWT；客人端使用 `pc_guest` Cookie 和 `X-Guest-Token` / localStorage 镜像
- 生产环境由 Nginx 提供静态文件并将 `/api/` 反向代理到后端

## 4. 后端业务模块

| App | 主要职责 | 核心模型/接口 |
|---|---|---|
| `organizations` | 多租户根 | `Organization` |
| `accounts` | 登录账号、角色、偏好 | `User`、`UserPreference` |
| `branches` | 分店主数据 | `Branch` |
| `staff` | 员工与跨店调动 | `StaffMember`、`StaffTransfer` |
| `paymentmethods` | 分店支付方式 | `PaymentMethodDef` |
| `dailyreports` | 实时报表和历史快照 | `DailyReport`、`DailyReportHistory` |
| `purchasing` | 供应商、进货、价格比较 | `Supplier`、`PurchaseRecord` |
| `inventory` | 商品、库存与不可变库存流水 | `Product`、`Stock`、`StockTransaction` |
| `dashboard` | 看板与月度分析聚合 | 无独立业务模型 |
| `scheduling` | 月度排班、可出勤意向、实际出勤 | `SchedulePeriod`、`AvailabilityRequest`、`Shift`、`ActualWorkRecord` |
| `wages` | `v2_simple` 时薪/临时工工资估算 | `WageRule`、`WageMonthlyClosing`、`WageEmployeeResult`、`WageDailyDetail` |
| `lottery` | 历史导入型抽奖名单管理 | 大饼、京都爱电王相关人员/批次/记录 |
| `promotions` | 新积分卡营销闭环 | 活动、客人、积分、打卡、消费确认、奖品、抽奖、券、里程碑 |

## 5. 角色和租户边界

### Organization

`Organization` 是最高层租户边界。管理员只能访问自己 Organization 的数据；分店账号只能访问自己的 Branch；业务数据通过 Branch、员工或直接 Organization 追溯到租户。

### 角色

| 角色 | 主要能力 |
|---|---|
| `admin` | 本组织内全分店管理、账号/分店/员工调动、营销配置、积分调整和消费确认作废 |
| `branch` | 本分店经营、日报、进货、员工、排班、工资及营销活动管理 |
| `staff` | 自助排班/出勤/工资相关能力，以及营销消费确认和券核销；不能访问一般经营数据 |

后端默认权限包含 `DenyStaffRole`，前端路由和菜单隐藏仅用于 UX，不能代替后端权限检查。

## 6. 新积分卡与抽奖闭环

### 6.1 业务主流程

```text
客人扫描店内固定码
  → 手机号登记并同意隐私告知
  → 获得稳定 card_token 与客人卡片
  → 结账时店员扫描卡片并输入消费金额
  → 服务端记录消费确认、打卡、积分和集章
  → 客人用积分抽奖或兑换下次使用的券
  → 下次到店由店员验证并核销券
```

信任边界是：客人端不能自行创建消费资格，积分和打卡只能由登录店员的消费确认产生；抽奖和积分扣减由服务端事务完成。

### 6.2 `promotions` 核心模型

- `Campaign`：分店营销活动和积分规则
- `Customer`：Organization 内按归一化手机号唯一的客人卡
- `PointsLedger`：赚分、花分、过期和调整的不可变积分流水
- `CheckInRecord`：按客人、活动、营业日去重的到店记录
- `SpendVerification`：店员确认的消费事实，普通更新/删除关闭，作废走独立 action
- `Prize`：权重、库存、每日库存、奖品配置和核销规则
- `LotteryDraw`：抽奖来源、结果、权重快照和幂等 `request_id`
- `Voucher`：抽奖、里程碑或积分兑换生成的下次到店券
- `Milestone` / `MilestoneClaim`：累计积分里程碑及一次性领取保护

### 6.3 主要 API

```text
POST /api/guest/register/
POST /api/guest/login/
GET  /api/guest/card/
POST /api/guest/redeem/
POST /api/guest/draw/

CRUD /api/promotions/campaigns/
GET  /api/promotions/customers/
POST /api/promotions/customers/lookup/
POST /api/promotions/customers/{id}/points-adjust/
POST /api/promotions/spend-verifications/
GET  /api/promotions/spend-verifications/mine/
POST /api/promotions/spend-verifications/{id}/void/
CRUD /api/promotions/prizes/
CRUD /api/promotions/milestones/
POST /api/promotions/vouchers/verify/
POST /api/promotions/vouchers/redeem/
```

### 6.4 前端页面

- `/pc/register`：客人登记
- `/pc/login`：手机号 + 生日的只读找回
- `/pc/card`：二维码、积分、集章、流水、里程碑、抽奖/兑换券
- `/kiosk/verify`：店员扫描客人卡片并确认消费
- `/kiosk/redeem`：店员验证和核销券
- `/promotions`：管理活动、奖池、里程碑、客人、记录
- `/lottery`：历史大饼/京都爱电王抽奖名单工作区

## 7. 两个“抽奖”模块的区别

### `lottery`

这是历史数据和名单管理模块，不是实时概率抽奖引擎。它维护大饼和京都爱电王两套独立数据结构，支持人员、门店/批次、记录、查询和 Excel 导出。

### `promotions`

这是当前正在建设的营销系统，包含消费确认、积分经济、权重抽奖、库存、券、里程碑和核销。后续业务开发应优先围绕它进行，不要把 `lottery` 的历史记录模型直接当作新营销抽奖模型使用。

## 8. 部署与运行

### 本地后端

```bash
cd store-admin-backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python manage.py migrate
python manage.py seed_demo_data
python manage.py seed_promotions_demo
python manage.py runserver 8071
```

### 本地前端

```bash
cd store-admin-frontend
npm install
npm run dev
```

### Docker

`docker-compose.yml` 包含 MySQL 8、backend 和 frontend。backend 镜像构建时只执行 `collectstatic`，迁移需要部署者显式执行；frontend Nginx 将 `/api/` 代理到 backend。

生产上线前必须配置真实 `SECRET_KEY`、`ALLOWED_HOSTS`、HTTPS、SSL Cookie、HSTS 和正确的前端 API 地址。

## 9. 当前质量基线

本次只读检查结果：

- `python manage.py check`：通过
- Django 全量测试：324 项通过
- `python manage.py test promotions`：65 项通过
- `npm run type-check`：通过
- `npx oxlint .`：通过
- `npm run build-only`：通过
- `pip check`：通过
- `makemigrations --check --dry-run`：未发现待生成迁移
- ESLint：2 个未使用导入错误，详见《项目检查报告》

## 10. 推荐维护顺序

1. 上线前先完成活动时间窗、奖品库存初始化、重复消费确认策略和公开接口限流。
2. 再补齐营销模块的风险记录、运营报表、客人保留期清理和退款联动。
3. 清理文档中“阶段 1/阶段 2 尚未实现”的旧描述，建立一份单一的实施状态来源。
4. 继续拆分前端大 bundle，并补充真实浏览器下的客人端、平板端和券核销流程验收。
