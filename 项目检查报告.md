# 店铺管理系统当前项目检查报告

> 检查日期：2026-09-01
> 
> 检查范围：当前工作区全部源码、配置、迁移、测试和 Markdown 文档。
> 
> 变更声明：本次没有修改任何代码；仅新增项目说明和本报告两份 Markdown 文档。原有未提交改动未回退。

## 1. 总体结论

项目当前处于“主系统已可运行，营销抽奖功能已完成较大闭环但仍需上线前收口”的状态。

主要优点：

- 前后端分层清晰，业务 app 拆分明确。
- Organization 多租户隔离和角色权限已有统一机制。
- 积分、抽奖库存、券核销等关键写操作使用事务和行锁。
- 当前 Django 全量测试 324 项全部通过，promotions 专项 65 项全部通过。
- 前端类型检查、oxlint 和生产构建通过。

主要问题：

- ESLint 当前有 2 个真实错误。
- 活动 `starts_at` / `ends_at` 字段保存了，但核心流程没有执行时间窗校验。
- 新建奖品时 `total_stock` 不会自动初始化 `remaining_stock`，可能把限量奖品变成无限库存。
- 同一营业日重复消费确认会重复发积分和集章，消费确认没有请求幂等键。
- 公开客人接口仍没有限流。
- 历史 `lottery` 模块对 `staff` 开放时，API 会返回完整手机号/联系方式，前端隐藏不等于后端脱敏。
- 方案、任务书和当前实现的阶段状态不同步。

## 2. 已执行检查

| 检查项 | 结果 |
|---|---|
| `manage.py check` | 通过，无 Django 普通检查问题 |
| Django 全量测试 | 324/324 通过，约 242 秒 |
| `manage.py test promotions` | 65/65 通过，约 44 秒 |
| `npm run type-check` | 通过 |
| `npx oxlint .` | 通过 |
| `npx eslint . --no-cache` | 失败，2 个未使用导入 |
| `npm run build-only` | 通过 |
| `pip check` | 通过 |
| `makemigrations --check --dry-run` | No changes detected |
| `manage.py check --deploy` | 6 项开发环境安全提醒，见第 6 节 |

测试期间出现的非失败警告：

- MySQL 不支持 `inventory.Product` 的条件唯一约束，该约束不会落到数据库，需要继续依靠 serializer 校验。
- 当前开发环境 JWT 使用的 SECRET_KEY 长度不足，仅是本地配置风险，生产必须替换。

## 3. 当前工作区状态

Git 状态显示存在多项未提交改动，主要包括：

- `dailyreports`、前端日报和 i18n 的迭代改动
- 新增 `promotions` app 与其迁移、测试、管理命令
- 新增 `lottery` app 与前端历史抽奖页面
- 新增客人端、店员平板端、营销后台、二维码组件
- 后端 README、路由和 settings 更新

这些改动属于当前项目状态的一部分。本次没有执行 reset、checkout、格式化或自动修复。

## 4. 高优先级问题

### P1 — 活动时间窗未在核心流程执行

`Campaign` 有 `starts_at` 和 `ends_at` 字段，管理端也能保存；但：

- `load_store_token()` 只检查 `status=active`。
- `verify_spend()` 只检查活动状态，不检查开始/结束时间。
- `_active_campaign_for()` 和 `draw_lottery()` 也只检查活动状态。

因此，一个状态仍为 active 但已经开始前或结束后的活动，仍可能登记、确认消费或抽奖。上线前应统一定义“活动有效”的函数，并在登记、消费确认、积分兑换、直接抽奖四条路径复用。

定位：`store-admin-backend/promotions/services.py`、`store-admin-backend/promotions/views.py`。

### P1 — 新建限量奖品的 `remaining_stock` 不会初始化

`Prize.remaining_stock` 默认为 `NULL`，在 serializer 中是只读字段；新建奖品时只接受 `total_stock`，没有在 `create` 或 view 的 `perform_create` 中把 `remaining_stock` 设置成 `total_stock`。

结果是：管理员新建一个 `total_stock=10` 的奖品后，`remaining_stock` 仍可能是 `NULL`，而抽奖逻辑把 `NULL` 当作无限库存。当前 demo seed 手动同时写入了两个字段，因此测试没有覆盖后台新建路径。

定位：`store-admin-backend/promotions/models.py:301-305`、`promotions/serializers.py:231-253`、`promotions/views.py:679-685`。

### P1 — 重复消费确认可重复发放积分和集章

`verify_spend()` 在同一客人、同一活动、同一营业日只复用 `CheckInRecord`，但每一次请求仍会创建新的 `SpendVerification` 并发放积分；`stamp_count` 也没有绑定 `ci_created`，而是每次确认都加 1。

这在“每一笔确认都代表一笔独立消费”的业务解释下可以成立，但在平板网络重试、店员重复点击或扫码器重复提交时，会产生重复奖励。当前专项测试还明确覆盖了“第二次确认仍发积分”，但没有覆盖“重复请求幂等”或“集章只按唯一到店增加”。

建议在业务上明确二选一：

1. 每笔消费都可独立累计：增加明确的消费/小票标识或更强的店员二次确认。
2. 一次到店只奖励一次：为消费确认增加 `request_id` 幂等机制，并仅在新建 `CheckInRecord` 时增加集章。

定位：`store-admin-backend/promotions/services.py:161-202`、`promotions/tests.py:100-112`。

## 5. 中优先级问题

### P2 — `max_draws_per_verification` 当前没有实际生效

活动模型保存了 `max_draws_per_verification`，但 `verify_spend()` 只在达到门槛时固定增加 1 次 `direct_draws`，没有使用该配置。若产品预期金额或活动配置可以产生多次直接抽奖，需要补充计算和测试；若第一版永远只给 1 次，应删除/隐藏该字段或明确其暂未生效。

定位：`store-admin-backend/promotions/models.py:41-44`、`promotions/services.py:155-159`。

### P2 — 公开接口无限流

客人登记、只读登录、查看卡片、积分兑换和抽奖接口均为 `AllowAny`，代码中保留了 `TODO(phase-3): throttle`。其中手机号 + 生日登录、积分兑换和抽奖属于可被自动化尝试的公开接口。

数据库缓存已经配置好，但尚未接入 DRF throttle 或其他限流实现。上线前至少应按 IP、card token、手机号和请求类型设置频率限制，并记录被限流事件。

定位：`store-admin-backend/promotions/views.py:139-294`。

### P2 — 历史 `lottery` API 的 staff PII 权限偏宽

`lottery.views.OrganizationScopedViewSet` 明确使用 `permission_classes = [IsAuthenticated]`，前端路由也允许 `staff` 进入 `/lottery`。但 `DabingPersonSerializer`、`DabingRecordSerializer` 等 API 字段包含完整手机号和联系信息，前端只是默认将其显示为后四位。

如果 staff 不需要历史抽奖客户资料，应在后端禁止 staff 或使用按角色不同的 serializer；不能只依赖前端 `showPhones=false`。即使 staff 被认为可信，完整个人信息也应按最小权限原则重新确认。

定位：`store-admin-backend/lottery/views.py:16-29`、`lottery/serializers.py:17-44`、`store-admin-frontend/src/router/index.ts:73-77`。

### P2 — 奖品配置缺少更完整的业务约束

当前 serializer 已按奖品类型校验 `reward_config`，但还需确认并补充：

- `remaining_stock` 与 `total_stock` 的变更策略
- `total_stock` 是否允许调小到已发放数量以下
- 面额、最小消费额、有效期是否允许为 0
- active 活动是否允许多个奖池并存
- 一个分店是否允许多个 active Campaign

这些不是当前测试失败，但会影响运营配置的安全性和可解释性。

## 6. 部署和安全检查

`manage.py check --deploy` 当前报告：

- 未启用 HSTS
- 未启用 SSL 强制跳转
- SECRET_KEY 仍是开发默认/长度不足
- SESSION_COOKIE_SECURE 未开启
- CSRF_COOKIE_SECURE 未开启
- DEBUG=True

这些与当前本地开发配置一致，不表示本地功能失败；生产部署必须通过环境变量和 HTTPS 反向代理逐项处理。特别需要在正式域名确定后重新生成固定二维码，因为店内二维码 URL 会写入域名。

## 7. 前端质量和性能

### 已通过

- TypeScript 项目引用类型检查通过。
- oxlint 通过。
- Vite production build 通过。
- 客人端、店员 kiosk、营销后台和历史抽奖页面都已进入路由和构建产物。

### ESLint 错误

1. `store-admin-frontend/src/components/CampaignPrizesDrawer.vue`：`Delete` 导入未使用。
2. `store-admin-frontend/src/views/PromotionsView.vue`：`todayJst` 导入未使用。

### Bundle 警告

生产构建报告两个较大 chunk：约 1.37 MB 和 0.93 MB，gzip 后约 448 KB 和 257 KB。当前不阻塞构建，但可以继续按 ECharts、Excel、PDF 和 Element Plus 等重量依赖拆包或延迟加载。

## 8. 文档一致性

当前文档存在明显的状态分层：

- `promotions-开发任务书.md` 仍以“阶段 1”为主，并将抽奖、奖品、券列为后续阶段。
- `打卡与抽奖实施方案.md` 的阶段表也把抽奖、券、里程碑列在阶段 2/2.5。
- 当前源码已经包含 `Prize`、`LotteryDraw`、`Voucher`、`Milestone`、客人抽奖页面和券核销页面。
- `store-admin-backend/README.md` 已部分记录 phase 2/2.5，但仍保留阶段性措辞。
- `开发进度.md` 最后更新为 2026-08-21，而当前抽奖/促销文件在 2026-08-31 附近新增。

建议建立一份“当前实现状态表”，逐项标记：已实现、已测试、未上线、待业务确认、后续规划，并将旧评审稿标记为历史版本。

## 9. 建议执行顺序

### 上线前必须处理

1. 活动时间窗校验。
2. 限量奖品库存初始化及编辑策略。
3. 重复消费确认和集章规则的最终决策，并补幂等/重复提交测试。
4. 公开 guest 接口限流。
5. staff 对历史 lottery 客户信息的后端权限确认。
6. 生产 SECRET_KEY、HTTPS、Cookie、HSTS、DEBUG、ALLOWED_HOSTS 配置。
7. 修复 2 个 ESLint 错误。

### 上线后优化

1. 风险事件模型、风险规则和运营报表。
2. 客人数据保留期限与定时清理。
3. 退款联动、生日券和 POS/API 对接。
4. 前端大包拆分和真实设备视觉/交互验收。

## 10. 其他项目/开发模块说明

本次对话中我只读取和检查了当前 `/Users/tatsuya/Documents/Projects/shop` 项目，没有读取或引用其他项目的代码，也没有发现另一个独立的抽奖开发项目。当前项目内部的其他开发模块主要是排班、工资、进货、库存、日报、看板和历史抽奖名单模块，已在本报告和《项目整体说明》中区分说明。
